# Control plane runtime dependencies. boto3/cryptography imports throughout
# src/ are deferred (see provisioner.py, registry.py) so unit tests of the
# orchestration logic don't need this installed - see requirements-dev.txt
# for the test-only extras.
boto3==1.34.144
cryptography==43.0.0
psycopg[binary]==3.2.2
# The onboarding HTTP surface (ROADMAP section 7 item 2). Pinned to the same
# versions apps/api uses - two FastAPI versions in one repo is a debugging
# session nobody needs.
fastapi==0.111.1
uvicorn[standard]==0.30.3
