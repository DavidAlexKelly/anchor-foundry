[pytest]
# `rootdir`-local so `api.py` and `conftest.py` import as plain modules rather
# than needing a package and an installed path. The suite is deliberately flat.
testpaths = .
python_files = test_*.py
addopts = -p no:cacheprovider
