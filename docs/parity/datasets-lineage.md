# Datasets and Lineage — parity specification

**Covers:** Dataset Preview, Data Lineage.

**Sources:** `foundry_dataset-preview.pdf` (39 pp), `foundry_data-lineage.pdf` (89 pp).

**Today:** `components/applications/dataset-app.tsx` with tabs Preview, Schema, History, Lineage, Details; dataset time travel at §56; lineage graph at §14.

We are closer to parity here than anywhere else. Dataset Preview is a 39-page product and our application already has the right five tabs.

---

## 1. Dataset Preview

Foundry's layout is header, information panel, tab views, preview table (`dataset-preview` p.2).

### 1.1 Header

| Feature | Status |
|---|---|
| Name, display name, location, selected branch | ◑ — no branch selector |
| Share, move, rename | ○ |

### 1.2 Information panel — three sections (p.3)

| Section | Feature | Status |
|---|---|---|
| **About** | created / updated time and user | ✅ |
| | table size | ◑ |
| | tools and input datasets used to create the data | ◑ via Lineage |
| | tags | ○ |
| | **Edit schema** — infer a schema for CSV and JSON | ◑ §362 — see §1.4 |
| | parsing options: drop jagged rows, change encoding, add file path / byte offset / import timestamp / row number columns | ◑ §362 — all but the byte offset; see §1.4 |
| **Columns** | type, description, **data stats — null percentage, distributions, samples** | ✅ §8 profiling |
| **Schedules** | configured build schedules | ◑ |

### 1.3 Tabs

| Tab | Feature | Status |
|---|---|---|
| **Preview** | sample table with light interaction over the full dataset | ✅ |
| **History** | job/build list with statuses and durations | ✅ |
| | **Summary view** — aggregated job statuses over time | ✅ §359 | (p.3) — thirty daily buckets of a model's runs, counted **on the server over a real window**. That is the decision the row turns on: `list_runs` is `LIMIT 50`, so a summary folded from the runs the page already has is a summary of the last fifty runs wearing the words "over time" — two days of it for a model that runs hourly. §323 had settled the same problem one resource over, and its rule is copied here: **the window comes back with the answer**, so a screen cannot hard-code a number that later moves. **By `queued_at` where §323 filters on `started_at`**, deliberately: a queued run has no `started_at`, so that window drops precisely the runs whose status is the thing being counted, and a model with a stuck backlog would show an empty summary. **Three counts, not five** — `succeeded`, `failed`, and `unfinished` for queued-and-running together, because in a bucket from three weeks ago the difference between a run still queued and one still going is not a fact about that day; §323 reports `running` alone for the opposite reason, being a snapshot of now. **No `cancelled`**, though the enum has had it since migration 0003: nothing writes it (there is no cancel endpoint — a synchronous SQL run has no meaningful cancel), and a count that can only be nought is a category that sits in every bucket and never appears (§214). **The gaps are drawn**: the server sends only the days that had runs, and the client fills the window, because a chart of the non-empty days puts Monday beside Friday at equal width and says the model ran steadily. **An empty window is a sentence, not thirty empty bars** — which say "this failed thirty times to run" as readily as "nothing happened" (§214). Drawn in CSS: thirty stacked bars is the whole of it, and adding a charting dependency is a decision every later chart inherits. **The sweep scored 24 and turned up something that was not this unit's.** A mutant reading the *local* date instead of the UTC one survived — twice. The first time the test's hour was wrong (23:30 UTC is still the same date in New York, so it proved nothing). The second time it was `apps/web/vitest.config.ts`: it has said `env: { TZ: "America/New_York" }` for as long as its comment has explained why UTC would make such a test meaningless, and **it did nothing** — `test.env` assigns `process.env` after the worker starts, and Node resolves the zone `Date` uses once, at startup. Inside a test `process.env.TZ` read `undefined` and `Intl` resolved `UTC`. So the whole unit suite had been running in the one zone that comment says proves nothing. Moved to `npm test`, which exports it before Node starts; all 2608 tests still pass, and the specific mutation the comment describes — deleting `timeZone: "UTC"` from `canvas/pure.ts` — is now caught by one more test than it was (`labels a day bucket by its own UTC date`, which could not fail under UTC). |
| | job detail: progress, specification, **build logs**, files, resulting schema | ◑ §358 | (p.3) — **build logs done; the rest of the row is still ○.** p.3 says "a list of jobs appears with their statuses and durations. Upon selection, a detailed Job view appears", and until §358 there was no list at all: `modelApi.runs` had existed since the models layer was written and **nothing on the front end had ever called it**, so the latest run's status badge was the whole of a model's history. **`model_runs.log_s3_key` has existed since migration 0003 and nothing had ever written it** — `services/models.py` said it was "written by the worker runtime for long runs", which was true of no runtime; §216 found the sentence and this is it becoming true, with no migration needed because the column was already there. **What was thrown away**: both runners already captured stdout and stderr and kept one line of it, truncated to 500 characters, as `error_message` — so a `print()` in a transform, the first thing anybody reaches for, went into the void. **Absent rather than empty** (§214): a quiet run stores nothing and the control says which kind of nothing it is, in four different sentences, because they send a reader to different places — not finished yet, a SQL transform with no output to capture, printed nothing, or here it is. **A log is not a summary**: the one-line `error_message` stays what it was, because the list view has no room for a traceback. **Capped at 64 KiB keeping the end**, since a transform that printed for a while and then raised has its answer in the last lines. **And one duration formatter, because writing this one made two**: `action-metrics` had `durationText` for an action's P95 — `ontology.md`'s row cites the page, and this document does not name that source, which is §302's check doing its job on this very sentence — and §358 wrote a second for a run's duration; they agreed everywhere except whole minutes, where one said "2m" and the other "2m 0s", so which spelling a reader saw depended on which page they were on. §298's finding in a different corner — there two wordings for one refusal, here two spellings for one number — found by re-reading before it shipped, measured rather than assumed, and now one function in `lib/duration` with **its own** test file: the differential test comparing the two callers agrees whenever they move together, which is the known limit `test_limits.py` records, and a mutant widening the seconds threshold walked straight through it. ○ on this row: *specification* (the code the run executed — `model_version` is recorded and not yet drawn), *files*, and *resulting schema*, which is already a click away on the output dataset. |
| | **Create a branch from a historical transaction** | ✅ §357 — (p.4) **the action was not missing; its place was.** Forking a version into an independent dataset has existed since migration 0025, but it lived on the datasets *list* behind a dropdown asking which version you meant, and p.4 puts it on the transaction you are already reading: "Choose a previous transaction from the left panel and select the ellipsis (...) icon to **Create branch**." So the version is not a question, it is the row you pressed — which is also the bug the placement removes, since a dropdown defaulting to the current version is one mis-click away from branching "as it is now" when you meant v1. **Two citations on this row were wrong** and §216 is why they were checked: time travel is **§96**, not §56 (§56 is the dataset application), and the ○ was wrong outright. **And it found a real defect**: forking a version whose bytes are gone raised `FileNotFoundError` — a 500 and a traceback on a button somebody pressed deliberately — which is the *same* failure §56 named and fixed on the read path, left unfixed one endpoint over. Verified with a throwaway probe before a line was written, rather than assumed. **Three states, not two**, now matching what the read path already said: a version nobody wrote is a 404; one whose `s3_manifest_key` is NULL is unaddressable rather than absent; one whose object is gone is a third thing again, and each gets its own sentence. p.4 scopes branching to transactions "that have not been deleted by a retention policy" and **this platform has no retention policy at all** (`docs/decisions/0005` — nothing deletes an old version today), so there was no policy to port; the state it guards against arrives here by other routes and the History tab already recognised it (`size_bytes == null` → "not stored"), which is what the disabled button reuses rather than inventing a rule. **The sweep scored 16/0 and re-swept 2/1 → 1/0 after a fix.** Two survivors in the first pass were the same hole — both about the NULL-key branch, which the tests could not reach because deleting the parquet leaves the key set. No write path this platform still has produces that state, so it is written directly with `ADMIN_DSN` the way `test_action_metrics` does; it is the shape of an old row, and the only honest way to make one is to write one. The adversarial pass then found one more: a Cancel that branched anyway **survived**, because the negative assertion raced the stray request it was looking for. It now waits on a real branch of the other version first (§318), and names the dataset Cancel would have made rather than counting — a count is wrong here anyway, since the earlier tests leave branches behind. ○: the disabled half of the button has no browser test and that is deliberate (§213) — nothing the browser can call removes a version's object, so the API owns that refusal and `whyNotBranchable` owns its wording. |
| **Details** | technical information and administrative operations | ✅ |
| **Schema** | schema view and edit | ✅ |

Streaming datasets add a Streaming tab and change History to appear only in Archive view (p.4). Out of scope — we have no streaming.

### 1.4 CSV parsing (TOC §3)

| Feature | Status | Notes |
|---|---|---|
| **Parsing options over an uploaded file** | ◑ §362 | (p.14, p.24-27) — delimiter, quote character, header, skipped lines, null markers, dropping rows that do not fit, encoding, and p.14's file-path / import-time / row-number columns, with p.24's **rehearsal**: Preview shows what the options would produce and writes nothing, and Apply keeps that same parse as a new version (§361's shape — the badly-parsed version stays readable). **It re-parses the file somebody uploaded rather than asking for options up front**, which is p.24's own flow: the Edit Schema UI is reached from the preview tab, where the parse that went wrong is on screen. That is only possible because **the bytes were already being kept and nothing could reach them** — the upload route has written the file verbatim to `{prefix}original/{filename}` since the first upload ever ran, with "export everything §11 includes what you gave us" beside it, and then the filename went nowhere: not on the row, and the export route serves the Parquet or a converted CSV instead. Every upload since has paid to store a second copy no code path could find. db 0090 records the name, which is the whole migration. **The options are not stored**, where Foundry stores them in the schema (p.24) — its datasets keep receiving files and an uploaded one here receives exactly one, so a stored option set would have no second reader; what was chosen goes in the audit entry instead. |
| **`TextDataFrameReader`'s remaining properties** | ○ | (p.25-27) — each for its own reason, none of them effort. **`parser`** (CSV_PARSER / MULTILINE_CSV_PARSER / SIMPLE_PARSER / SINGLE_COLUMN_PARSER) has no DuckDB counterpart to select. **`dateFormat`** is a map from column name to pattern; DuckDB's `dateformat` is one pattern for the whole file, so offering it under Foundry's name would be a narrower control wearing a wider label. **`recordDelimiter`** maps to DuckDB's `new_line` and that part works — but every file that could show it doing something turned out to be one DuckDB already handles, which is the `escape` situation below a second time. **`initialReadTimeout`** guards a distributed read this platform does not have. The **Spark `DataSourceDataFrameReader` passthrough** (p.27-28) needs Spark. |
| **Byte offset for a row** | ○ | (p.14) — the one of p.14's four added columns that is missing, and it is missing because DuckDB does not expose one: `filename=true` gives the path and a window function gives the row number, and there is no equivalent for where a row began in the file. |
| **`escape`** | ○ — **withdrawn** | (p.26's quoting) — it was built, and then no file could be found where passing it changes DuckDB 1.1.1's answer: the sniffer already reads `"a\",b"` as `a",b` with and without it, and DuckDB refuses an escape longer than one byte anyway. A control whose mutant nothing can catch is a control that cannot be shown to work (§213), and one that looks like it works is worse than one that is absent (§214), so it was removed rather than shipped untested. It comes back with the file that needs it. |
| Schema edits for non-CSV file types | ◑ | (p.14's last sentence) — the Schema tab exists (§1.3); what it does not do is re-read the file. |

**A latin-1 file cannot be uploaded at all today, and p.14's "change encoding" is what fixes it.** This is worth stating plainly because it is not a nicety: DuckDB 1.1.1 has no `encoding` parameter on `read_csv` (it arrived in 1.2), and such a file does not parse badly — it fails outright with "Invalid unicode". So the bytes are decoded in Python before DuckDB sees them, which is the only thing that makes the file loadable at all, and the option is honest on the version actually deployed rather than waiting for an upgrade to mean something.

**Everything here was measured against the parser rather than assumed.** `read_csv_auto` is better than it looks: it finds `;`, `|` and tabs on its own, reads single-quoted fields, and skips a `#`-commented preamble. Every test file in `test_dataset_parsing.py` therefore had to be one it genuinely gets wrong — a `^` delimiter, `|` quotes, a preamble with two fields per line like the data, a jagged row that makes it abandon the delimiter altogether and return one column with no error at all. A test built on a file the sniffer reads correctly would have been a check that cannot fail.

---

## 2. Data Lineage

Foundry's navigation has six regions (`data-lineage` p.6): lineage graph, branch settings, side panel, node details panel, graph tools, save graph.

### 2.1 Graph

| Feature | Status | Notes |
|---|---|---|
| Interactive graph of resources | ✅ | §14 |
| **Expand a node's related resources** via arrows on either side | ✅ §355 | (p.7, p.52) — **selection, not addition, and that is the whole translation.** Foundry's arrows *add* related resources because its graph is built up from nothing over an enterprise; this graph is a project drawn whole, so there is nothing to add and the same gesture grows the **selection** instead. The control keeps what it was for — "show me what this dataset feeds" — and drops the mechanism it only needed because the graph started empty. It also lands on §354: expand downstream from a source and p.55's Frequent Columns is answering about everything it touches, which is the impact-analysis question §2.4's row opened. **Four buttons, not arrows on the card**: the cards are 190px wide with three lines of text on them, and an arrow small enough to fit beside one is an arrow nobody hits. **Two granularities, because p.52 has two** — one hop is p.7's single arrow, the whole chain is p.52's double arrow above *Expand parents* — and **one direction at a time**, because from the middle of a pipeline "what feeds this" and "what does this feed" have different answers, and walking both is the whole component, which is what `focus` already draws. **Edges, not links**: an ontology link type is a relationship rather than a direction data flows, so two object types referring to each other are not one upstream of the other (§351's rule read the other way round). Breadth-first with a `seen` set, so a cycle terminates — this graph reports cycles rather than removing them (§352). |
| Auto-layout, with manual drag and a **Layout all nodes** reset | ◑ | (p.11) — ○ within the row: p.11's *other* layouts (vertical, hierarchical, by level), which Foundry unlocks **when several nodes are selected**. That prerequisite arrived with §354, so the gap is now the arrangements themselves rather than anything to arrange them over. |
| **Selection tool** — Select All, Invert selection, Select children, Select parents | ◑ §355, §356 | (p.12) — three of the four are here under other names, which is worth saying rather than claiming the row: **Select children / Select parents** are §355's Expand buttons (one hop down, one hop up, from the current selection), and **Select All** is §356's select-every-match, which is the same act over a question rather than over the whole graph. **Invert selection is the one with nothing behind it**, and it is not a rename of anything — it is the only one of the four that answers "everything except what I am looking at", which is how somebody checks what a selection *left out*. Cheap when it is built: the graph already has every node id and §354 already owns `toggleSelected`. |
| **Find by column name** | ○ | (p.11) — p.11's Find searches "the name of the node or column names in datasets", and §356 built the first half. The second is a gap with its supply already in place: §353's Frequent Columns reads every dataset's schema off the same rows the graph draws, so finding the datasets that have a column is the index it already builds, asked from the search box instead of the histogram. |
| Pan mode and **drag-select mode**; multi-select with Ctrl/Cmd+click | ✅ §354 | (p.7, p.54) — **p.54 is why this was not cosmetic.** §353 built p.55's Frequent Columns over the graph as drawn, which is p.54's *first* instruction ("ensure you added all datasets of interest in your pipeline to your lineage graph") with the second one missing; "in your selection" appears three times across pp.54-55 and now means something. **Narrowed on the page, not on the server**: the server already sends which datasets hold each column, so narrowing is an intersection over data the page has — a round trip per rectangle would lag behind the drag that caused it. **Nothing selected is the whole graph** rather than an empty histogram, because that is the state the page opens in and a panel that stays blank until you have drawn a rectangle is one most readers never see working; a selection of things that are *not* datasets does come back empty, which is the honest answer rather than a fallback. **The rectangle is arithmetic and lives in `lib/`**, with the card geometry, because two copies of the layer arithmetic is a marquee that selects the node above the one it is drawn over and nothing on the screen says which copy is wrong (§191). **Overlap, not containment**: a rectangle that visibly covers most of a card has selected it, and requiring the whole card means dragging past the edge of a graph to pick up the node you are looking at. **`DRAG_FLOOR` is not a nicety**: a press on a card in Drag select mode is both gestures at once — the card's own handler runs *and* a zero-sized rectangle closes over it — and the two cancel out, leaving the node a person clicked unselected. **The sweep scored 28/3 and re-swept 3/0**, and all three survivors were tests that could not fail — the code was right and the checks were theatre, which is the other half of §213. Two were the same mistake about the canvas transform: a rectangle drawn around the **top** card still overlaps the right card whether or not the zoom is divided out, because the error is proportional to the distance from the origin and at the origin there is none. Drawn around the **last** card at 70% it selects the neighbour instead, and the same test panned rather than zoomed catches the other term. The third was a behaviour with no test at all: committing the rectangle when the pointer leaves the viewport, which is p.7's own case — the graphs Drag select is for are bigger than the window — and the claim in the comment beside it stood on nothing until a drag that exits through the bottom edge was written. |
| **Node colouring** | ○ | TOC §9 |
| Graph elements reference (shapes and states) | ○ | TOC §10 |

### 2.2 Side panel

| Helper | Status | Notes |
|---|---|---|
| **Search & Browse** — free-text or tree, add resources to the graph, add all results with or without sub-folders, advanced filters and sort | ◑ §356 | (p.8) — **find carries over; add does not.** p.8's helper does two things, and the adding is only there because Foundry's graph starts empty over an enterprise; this graph is a project drawn whole (§355 settled that), so every result is already on it. That leaves *finding* one by name — still real on a graph with forty cards — and turns "add all search results" into **select all of them**, where §354's histogram and §355's expansions carry it further. **Name and slug**, because the slug is what somebody writing a transform or an action against an object type types and it is the line the card already shows them (§351). **Nothing matches nothing**: an empty box with no kind chosen finds no results rather than every node, because that is the state the page opens in and a search that starts by highlighting the whole graph has said nothing — but a kind on its own *is* a question ("show me the models"), so it answers. **One dimming language**, shared with p.55's column highlight: dim means "not in what you are looking at", and two questions at once narrow rather than compete. **And a match is outlined as well as everything else fading**, because filtering to a kind that covers the whole graph dims nothing at all — a reader would be looking at forty undimmed cards wondering which ones the count meant (§214, and §356's sweep caught it: the first version of that test asserted the `data-` attribute rather than the mark it stands for). ○, each for a structural reason rather than effort: the **tree browse** and **sub-folder recursion** have nowhere to go, since there is no folder hierarchy under a project; **sort** has no result *list* to sort, because the results are the graph; and p.8's per-resource **Actions menu** belongs with the properties helper. **The sweep scored 17/0 after two corrections.** One survivor was a real gap and one was a bad mutant, which is the case for re-reading a survivor before believing it. The gap: every slug in this repo is derived from its name, so a build that searched *only* the slug found exactly the same nodes as one that searched both — there is a node now whose name and slug share nothing (`Refunds` / `rf-2024`), which is the only arrangement that can tell the two apart. The bad mutant: `.map((node) => node.id);` ends `nodesInRect` as well as `search`, so a first-occurrence replace mutated the wrong function — §69 hit exactly this and drew the wrong conclusion from it, rewriting tests that were fine because a replace aimed at `preview_transform`'s sandbox landed on `run_transform`'s. Re-aimed, it is caught — and the accidental mutant turned out to be a **non-finding worth keeping**: sorting `nodesInRect`'s output fails no test, correctly, because a rectangle has no order a reader chose, and that is now said in its docstring so a consumer that comes to need one asks rather than assumes (§213). |
| **Properties and Histogram** | ◑ §353, §354, §355 | (p.8) — the single-node half is the graph's detail bar and the multi-node half is Frequent Columns, over a selection (§354) that p.8's **Update selection** can now be drilled down into (§355): clicking a column highlights the datasets that have it, and the button narrows the selection to exactly those — the highlight is a question and this is the answer being kept. **The chip stays pressed and every remaining node stays lit**, because they all have the column; that is the drill-down having happened rather than a highlight that failed to dim anything, and clearing it as a side effect would leave a reader unable to see which column they drilled into one click after choosing it. ○: p.8's histogram is of property *values* ("common properties and their values alongside the number of appearances of each value") and this counts column **names**, which is p.54's own feature rather than p.8's. Not a side panel either: both halves sit on the graph, because this platform's graph is a page rather than an app with six regions. |
| **Manage Builds** | ✅ §386 | (p.9) — **the row was uncited and the section's (p.8) does not cover it** (§379); the builds helper is on p.9. It "offers you three build strategies: build only selected datasets, build all datasets between the selected datasets, build the selected datasets and all of their ancestors". Running a model exists (`POST /models/{id}/run`) and §354 gave the graph multi-select, so the pieces are there and **the gap is that the graph is not a place you start work from** — every build here begins on a model's own page.<br><br>**And the cascade this platform already has runs the other way** (§383). `enqueue_due_upstream_models` (worker step 2, db 0021) queues a run for every `trigger_mode='upstream'` model whose *inputs* have gained a version since its watermark — its own docstring calls that "what turns isolated transforms into pipelines". That is propagation **downstream**, after a build. All three of p.9's strategies reach the other way: *only selected* is a loop, *all between the selected* is a path query, *selected and all their ancestors* is an upstream reach. The cascade solves none of them, which is worth saying plainly because "the platform already cascades" makes this row sound like the loop is the work, and the loop is the one part that is trivial. **The two interesting strategies are selection queries**, and `services/pipeline.py` returns both halves they need already: `edges` (what flows into what) and `layer` from `_layer` (Kahn's, "every edge points strictly rightwards"), so the order to build an ancestor set in is on the client before anything is written.<br><br>**The interaction to settle before drawing the control**, which neither document says on its own: build a selection's ancestors and every `trigger_mode='upstream'` descendant fires *again* on the next worker pass, because each ancestor's build writes a new output version and that is an input version to whatever reads it. An ancestors build therefore produces more builds than the strategy names. Whether that is right or wasteful is a design question, and it is cheaper answered now than discovered afterwards. **The converse is why the strategy still earns its place**: a model whose trigger mode is *not* `'upstream'` never cascades at all, so for those an explicit ancestors build is the only thing that brings them current.<br><br>**Built in §386, and two of the three selections were already on this bar** — which is the sizing this row kept getting wrong in both directions. §354's multi-select *is* p.9's first strategy, and the `All upstream` chip is `relatives(edges, selected, "upstream", Infinity)` with the seeds included, which *is* the third. So what was added is the one selection that was missing (`Between`, the intersection of an upstream walk and a downstream one) and the build itself. Not three buttons: here the strategies are three ways to shape a selection, the shaping is a step the reader can watch happen, and two of the shapes were one click away already.<br><br>**What a build runs is a model and p.9 says datasets**; they meet one hop up — a selected model is itself, a selected dataset is the model that writes it, an uploaded dataset has no such model. That last is this row's §214 case, which is why the summary sits beside the button rather than inside it: it counts *transforms*, and names what will not be built. Built one at a time in `layer` order, because a transform reads its inputs' current versions. The cascade above is reported before the click rather than discovered in the run history.<br><br>**The sweep found the claim this row is about untested.** Building only the first model of the plan, and building in reverse order, both passed every browser test: nothing had ever pressed Build with more than one transform to run. One test now asserts both that every counted transform ran and that the upstream one finished before the downstream one started. |
| **Manage Schedules** | ✅ §387 | (p.10) — same uncited row, real source p.10: "set and edit build schedules for selected resources on the graph". **Build schedules exist** — a model carries `trigger_mode='cron'` and the worker's `scheduled_model_runs` job runs it (db 0024) — so the ◑ is right, and what is missing is the same thing the row above is missing: setting them *from the graph*, over a selection, rather than one model at a time. **Built in §387**, and p.10's caveat turned out to be the one half with nowhere to go: "the schedules apply to the branches (including fallback branches) configured in the graph", and this platform's pipeline graph has no notion of a branch — so there is no branch for a schedule to apply to, which is a difference in what a graph *is* here rather than a missing control.<br><br>**The same plan as the builds helper**, which is what made it small: which models a selection means is one question, and §386's `buildPlan` answers it — it carries each model's `trigger_mode` now rather than a second resolver being written beside it (§292).<br><br>**Overwriting is the behaviour and the summary says so before the click.** p.10 says "set and edit", so a selection holding already-scheduled models is ordinary rather than an error; replacing three schedules while somebody believes they are setting one is §214's shape, so it reads "schedule 3 transforms, replacing 2 existing (a, b)" and names them while there are few enough to be worth naming. **Clearing is `trigger_mode` back to `manual`**, and the expression goes with it — though *that* guarantee is the server's: `models.update` drops `cron_schedule` on any trigger change, which a sweep established by showing the client field could not matter. The Clear button is **absent rather than disabled** when there is nothing to clear.<br><br>**The cron box is shape-checked and not validated**: five fields, because `lib/cron.py` parses the expression with `croniter` and refuses it by name, and a second validator here would be a second opinion that disagrees the first time either changes (§191). `99 99 99 99 99` passes the shape check on purpose and is refused by the server.<br><br>**Nothing filters on where a model is authored**, which looks like an omission and is not: a repository-authored model refuses direct edits to its *code and inputs* only, because "trigger mode, schedule and health policy are how and when it runs... Gating them would make a project that requires review unable to pause a job." Checking that before building removed an exclusion the design would otherwise have carried. |
| **Related Artifacts** | ○ | (p.10) — same uncited row, real source p.10: "displays artifacts directly linked to the nodes selected on the graph… deleted and automatically saved files are excluded unless chosen otherwise". An *artifact* is a Foundry resource kind this platform has no counterpart for, and the exclusion clause names two states — deleted, and auto-saved — that need a trash and an autosave history, neither of which exists (the trash is the same absence `code-repositories.md`'s trashed-datasets row records). So this is a row about resources that do not exist here rather than a panel nobody drew. |

### 2.3 Beyond the graph

| Feature | Status | Notes |
|---|---|---|
| **Branch selector, with fallback branches in order** when a branch does not exist for a resource | ○ | (p.8) — a genuinely subtle behaviour worth copying exactly |
| View dataset preview and logic from the graph | ◑ | TOC §11 |
| **View build timeline** | ○ | (TOC §12; `data-lineage` **p.10**) — **cited to a table-of-contents entry rather than to the page that describes it** (§383), which is §379's shape again and was found the same way: reading p.9-10 whole for the two rows above. p.10 puts it among the node-detail tabs — "Build timelines: a Gantt chart of actual build time for the selected datasets" — beside Preview, History, Code and Data Health. §191 asked whether a row for it already existed before one was added, and it did: this one. The mark is right and nothing partial is built; `model_runs` records `started_at`/`finished_at` per run, so the data a Gantt chart needs is there and the absence is the chart. |
| **Understand out-of-date datasets** | ◑ §352 | (TOC §13; `data-lineage` p.51) — p.51 asks three questions and **two of them are answerable here; the third is named rather than guessed at**. "Is my dataset build failing?" was already on the graph, as a model's `last_run_status`. "Is there an upstream dataset that hasn't built and isn't up to date?" is what §352 built. "Have we received up-to-date data from the source?" needs an expectation about how often data *arrives*, and this platform has nowhere to record one — inventing a freshness target would put a number on the screen that nothing stands behind, so an uploaded dataset with nothing feeding it reports nothing. **Times, not version numbers**: two datasets' version counters are independent, so "input at v7, output at v3" says nothing at all; when each last *became what it is* is the comparison that means something. `built_at` is the latest version's `created_at` and deliberately **not** `datasets.updated_at`, because a rename touches that one and a rename is not a build — comparing those would mark everything downstream of a renamed dataset stale, which is a warning nobody can act on. **Two reasons rather than one flag**, which is p.51's own two questions: `input_is_newer` names *this* dataset as the thing to rebuild, `upstream_is_out_of_date` says the thing to rebuild is further up — a single "out of date" would have somebody rebuilding the wrong one and watching it come back stale. The direct reason wins when both apply, because it is the more specific. **Unbuilt is not stale**: a model that has never run has an output with no version, and reporting that as out of date would put a warning on every pipeline the moment it is drawn — one says press run, the other says press run *again*. The downstream walk is breadth-first with a `seen` set, so a cycle terminates rather than spinning (`_layer` reports cycles; it does not remove them). **Not on the Mermaid export**, and that is a decision rather than an omission: `lineage_for_dataset` emits names only and carries no node attributes at all, so there is nowhere for a staleness flag to go — §351's "check both builders" rule asked the question and this is the answer. ○: p.52's *Expand node* / *Expand parents* controls, which belong with the node-expansion row

**The sweep scored 11/3 with one no-op, and re-swept 12/2 after three corrections.** All three survivors were the same shape — a branch nothing could reach — and they were resolved three different ways, which is §213's whole point. **One was deleted**: the downstream walk carried an "unless it already has a reason" guard, and `seen` already is that check, because it starts as the directly-stale set and so every node the walk reaches is one nothing has marked. **Two stay, with the reasoning in the docstring rather than in a test that pretends to exercise them**: the `None` guards on `built_at` cannot be reached, since `models.output_dataset_id` is NULL until the first run and a never-run model therefore contributes no output *node* at all — but `None > datetime` raises, and a `TypeError` on a read path is a 500 rather than a wrong answer, which is the stated exception §213 makes. **And one test was claiming a state that cannot exist**, which is what the sweep actually found: it was written as "a dataset nothing has built yet is unbuilt rather than stale", and there is no such dataset. It asserts the shape of the graph now instead, and its first draft of *that* was wrong too — a never-run model has no *output* edge, but its input edge is there. **A fixture that sounded stronger was weaker**: re-running two models rather than one makes every stale dataset directly stale, which quietly removes the only node in the project that exercises the transitive reason at all. |
| **Find datasets with a given column** | ◑ §353 | (TOC §14; `data-lineage` p.54-55) — p.55's **Frequent Columns**: the columns on the graph, most frequent first, and clicking one highlights the datasets that have it. **Which datasets, not how many** — the count is what the section is *sorted* by and the list's length is that count, while the ids are what make p.55's click possible at all. **By name only, not by name and type**: p.55 says "by name", and a column that is a string in one dataset and an integer in another is exactly the case somebody changing a schema is looking for, which keying on the pair would hide. **The selection is the graph as drawn**, which is a decision rather than an omission: p.54 selects datasets with drag-select first, this platform's graph has single selection, and multi-select is its own ○ row in §2.2 — so the *graph* is the selection, which is p.54's own first instruction ("ensure you added all datasets of interest in your pipeline to your lineage graph") and leaves the narrowing to the row that owns it. Computed **after** the focus narrowing, for the reason §351's links are: a lineage view asks about the datasets it drew. **The highlight dims the rest rather than only colouring the lit ones**, because what a reader is looking for is which of forty nodes has the column, and clicking the same column again clears it — a highlight nothing can turn off is a mode rather than a question. No migration: `datasets.table_schema` is the current schema and the graph already reads that row. ○: p.54's drag-select and multi-select, on the §2.2 row that owns them, and the rest of p.54's *histogram* — this is the Frequent Columns section of it, not the value distributions

**The sweep scored 9/5 and re-swept 2/2**, and the five split three ways — which is the case for writing mutants against the *code* rather than the tests, since a list drawn from the test file would have produced none of them. **One was a bad mutant**: `columns = [] or _frequent_columns(...)` evaluates to the call, so it changed nothing; re-run as `columns = []` it is caught. **One was a missing test**: the fixture's three columns had three different frequencies, so nothing exercised the name tiebreak at all — there is a project now whose two columns are equally common and declared in non-alphabetical order, which is the only arrangement that can tell a tiebreak from the absence of one. **Three were unfalsifiable**, and stay with the reasoning in the docstring rather than a test that pretends to reach them: the `isinstance` on a schema row and the `not in` before appending guard shapes the write path cannot produce — db 0003's rows are objects, and DuckDB renames a CSV's duplicate column (`id, id` arrives as `id, id_1`), which was checked rather than assumed — and the `kind == "dataset"` filter is a statement of what is counted rather than a check, since an object type's id is not a dataset id and the lookup would miss anyway. |
| Build datasets from the graph | ○ | TOC §15 |
| **Roll back a dataset** | ◑ §361 | (TOC §18; `data-lineage` p.73, p.75-76) — p.75's **Rollback to transaction**, on the History row you are already reading, which is where §357 put Branch and for the same reason: the version is not a question, it is what was pressed. **A rollback appends a version; it never rewinds the pointer.** That is this repository's convention already — `models.restore_version` (db 0024) and `ontology.restore_type_version` (db 0028) both record a restore as a *new* version — and it is the better of the two readings, because a model run stamped with v7 has to keep resolving to what v7 was however many times somebody has rolled back since. Foundry crosses the skipped transactions out instead (p.70, p.76); the new version says where it came from, which is the same information written forwards rather than by editing the past. **No bytes are copied and no migration was needed**, both because the ground was already prepared: every version has always been written to its own key with nothing overwritten (`version_location` says so), so rolling back to v3 writes a row pointing at v3's file; and `produced_by_kind`/`produced_by_id` already mean "what produced this version", so a rollback fits them without a parallel provenance column. Two consequences worth naming — it costs nothing on a large dataset, and **it is undoable**, by rolling back to the version you were on. **Which is why p.76's warning is not copied.** Foundry asks you to acknowledge that "a rollback cannot easily be undone"; nothing here is deleted, so repeating that would be §214 pointed the other way — not a control that looks like it works, but a caution that looks warranted. What p.74 says about the *logic* is carried over, because it is true here too (rolling back a model's output does not touch the model, so the next build overwrites it) and it is shown only on datasets something actually rebuilds — a model output or a sync — since a warning about "the next build" on an uploaded file is a sentence the reader has to work out does not apply to them. **Editor**, which is p.74's own rule. p.74's "you are only able to roll back to a successful transaction" needs no check: a `dataset_versions` row exists only for a write that committed. p.72's "uploaded datasets are not supported" is deliberately **not** carried over — Foundry excludes them because there is no logic to rebuild from, and here an earlier upload is exactly what somebody who uploaded the wrong file wants back. ○ within the row: **forcing a snapshot on the next build** (p.73, p.77), which is an incremental concern, and incrementality here lives in connection syncs (`dataset_engine.merge_incremental`) rather than in transforms — so its counterpart is "make the next sync a full one", a different feature in a different file. |
| **Roll back a pipeline** | ○ — **not planned** | (TOC §17; `data-lineage` p.64) — **the source is retiring it.** p.64 marks it *planned deprecation*: "will be unavailable for use after November 30th. We recommend using the roll back a dataset feature instead." §216 found this by opening what the build-order line cited, and the line had it as the second half of a pair. Copying it would be copying a feature Foundry is removing, so it is ○ by decision rather than by backlog. **What it is for is answered here anyway, by a different mechanism.** p.64's motivating cases are an upstream mistake and bad data pushed in from a source; a model set to build on upstream change reacts to the rolled-back input like any other new version, so the downstream datasets are *recomputed* from the restored data rather than rewritten to matching historical transactions. That is asserted rather than claimed (`test_a_rolled_back_input_rebuilds_what_it_feeds`), and it is the stronger of the two where the logic has changed since — Foundry has to warn about exactly that case (p.71: "Rolling back this dataset will not revert logic changes"). What it does not give is p.65's **excluding a downstream dataset from the rollback**, which only means something when the downstream data is being rewritten rather than rebuilt. |
| Check resource permissions from the graph | ○ | TOC §19 |
| **See the impact of marking changes** | ○ | TOC §20 — which downstream consumers a permission change would break |
| **Save and share a graph** | ◑ §360 | (TOC §8; `data-lineage` p.12) — p.12 names three mechanisms and this is two of them. **Save / Open** is a saved view, stored by migration 0089, which follows db 0040's saved searches rather than inventing a second convention for a saved thing: **the view, never the nodes** (a lineage graph is a live question, and §355 settled that this platform draws the whole project, so the node list is not a choice anybody made), **shared within its scope** rather than private, **`jsonb` rather than a column per parameter** (this view grew three times in one session — §353's column, §354's selection, §356's search and kind filter — and a column apiece would have made each a migration), and **one name per scope**. It is **project-scoped where 0040 is workspace-scoped**, which is a difference with a reason: object types are workspace-wide so a search across them is too, while a lineage graph is over datasets and models, which belong to a project. **The quick share link is not a new button**, and that is the whole of that half: `CopyLinkButton` already copies the address bar, with the reasoning written beside it — "a button that rebuilt the link from component state could disagree with it" — so the work was making the URL *true*, which also fixed something this page had since it was written (a reload lost everything, because none of it was anywhere). **p.12's "read-only access" is read rather than copied**: the link reproduces a view, it does not admit anybody to the project, and one that widened access would be a new sharing mechanism and a security decision rather than a convenience. **The `focus` grammar moved into `services/saved_graphs` so the graph route and the save path cannot disagree** — db 0040's own lesson, since a view validated at open time means the person who finds out is not the person who made the mistake. ○ within the row, three ways: **Export graph to SVG**, which is its own unit and larger than it sounds — the edges are SVG but the nodes are positioned HTML, so exporting means redrawing the cards rather than serialising what is there; **re-saving over a graph that exists**, which today is a 409 reported where the name was typed, so the way to revise a saved view is to delete it and save again; and **p.12's branch paragraph** ("Your branch choice is saved with your saved graph… you will be asked if you would like to switch"), which has nothing to save yet — `services/pipeline.py` has no notion of a branch and the **Branch selector** row above is ○, so storing a branch now would be storing a fact the graph cannot use. When that row lands, the saved view gains a `branch` key and p.12's prompt becomes reachable.

**The sweep scored 49 mutants across three stages — and the first stage's first run was worth nothing at all, which is the finding.** It reported 23 for 23; `--timeout=120` is not an installed pytest plugin, so pytest exited 4 before collecting a single test and every mutant "failed" for that reason. §213 turned on the harness itself: a sweep that cannot survive is not a sweep. It now runs the unmutated tree first and refuses any exit code outside {0,1}, and an exit 1 with no `FAILED` line. Run honestly, the same 22 mutants scored **11/11**, then 22/0 after eleven tests — among them the two role checks (a viewer could have been allowed to save, or to delete a graph shared with everybody), the name that is stored untrimmed (`"Fleet "` and `"Fleet"` are the exact pair the one-name-per-project rule exists to prevent, and the difference is invisible in the list that shows them), and the project scope db 0089's header argues for at length and nothing checked. The vitest stage scored **14/1** — the survivor was `?kind=`, the one repeatable key where a blank value survives into a *list*, so the filter is not empty but a filter for a kind called "" — and the browser stage **10/3**. Two of those three were parts of a view no browser test carried (the kind filter and the column highlight, now both saved and reopened in one test), and **the third was not a missing test but dead code**: a build with the page's `url.set` on Open removed passed everything, because the remounted graph reports its view on its first effect and *that* is what writes the URL. The page's copy was the second writer of one fact, and the worse of the two — it would have written a kind this build cannot draw, where the graph's report passes it through `kindsIn` first. Deleted, with the finding in its place. **The same reading found a route with no caller**: `GET /saved-graphs/{id}` and the `modelApi.savedGraph` that was its only client, neither of which the Open dialog needs, since the list returns each view whole. Both gone — §358 shipped a `modelApi.runs` nobody called, and this is that trap a second time. |

### 2.4 Ontology entities in lineage

"Explore artifacts and ontology entities" (TOC §7; `data-lineage` p.30-32). Foundry's lineage graph is not dataset-only — object types and their backing datasets appear in the same graph. ◑ §351.

**Built:** an object type a project's dataset backs is a node on the graph, and the sync is an ordinary **edge** into it — a sync reads the dataset and writes the type's instances, so the type is downstream of it exactly as a model's output dataset is downstream of the model. A type backed by several of the project's datasets is **one** node with an arrow from each (db 0003 allows it), reporting the **worst** of their syncs: the question this view answers is "what is wrong downstream of here", and an `ok` beside an `error` would answer it with the reassuring half. p.32's **link types are drawn beside the edges, never among them** — `edges` is what the layering and the cycle report are built from, and a link type is a relationship rather than a dependency, so putting one there would report two object types that reference each other as a cycle in a *pipeline*, which is an ordinary and correct ontology. They are also resolved **after** the focus narrowing, so a link cannot drag a dataset into a lineage view that has no data path to the focus; both ends must already be on the graph. An object type is a node the graph can be **centred on** too, because a node a view draws and cannot centre on is one whose neighbours are unreachable from it.

**The boundary is the one this graph always had, stated for the ontology:** `object_types` and `link_types` are *workspace*-scoped and this graph is a *project's*, so a type reached through another project's dataset is not this project's lineage — the same line `_validate_and_set_inputs` draws for a model's inputs. `object_type_sources` has no project of its own; it is project-scoped by the dataset it names.

**Both builders, because there are two.** `pipeline.project_graph` is what the browser draws and `models.lineage_for_dataset` is the Mermaid/JSON export, and a feature added to one of them is a feature half the product does not have — the kind of split §216 exists to catch. Object types are in both; **link types are deliberately only in the first**, because the export's question is "what touches *this dataset*" and a link type joins two object types rather than touching any dataset.

**○ — p.30's Related artifacts panel**, and the reason is structural rather than effort: it lists "Contour visualizations and Slate applications", and this platform has neither. Building a panel with a badge counting nothing would be §214's control that cannot work.

**The sweep scored 17/1 with one no-op, and re-swept 3/0.** The survivor was a fixture that could not tell the difference: `sites` had two sources and both were `never_synced`, so the mutant that stopped folding them survived — with two identical states there is nothing a fold does that not folding does differently (§213). One source is synced now, which makes the two halves of the fold answer from *different rows*: a build that took whichever row came last would report either `ok` with a timestamp or `never_synced` with none, and the node reports `never_synced` **with** a timestamp. The no-op was a mutant whose anchor had drifted from the SQL, re-run separately rather than counted as a catch. And the harness tripped §350's own lesson twice — the browser stage's freshness guard fires once a sweep has restored anything under `apps/api/src`, so the server mutants are scored on the pytest stage and the stack is restarted before each baseline.

---

## 3. Build order

1. ~~**Ontology entities in the lineage graph.**~~ — **done (§351)**, and it turned lineage into the impact-analysis tool this line said it would. What §2.4 now records that this line could not have: the interesting decision was **not** which nodes to add but which arrows *are not edges* — a link type between two object types is a relationship, and `edges` means data flows this way. Struck in the commit that finished it, per §216. p.30's Related artifacts panel stays ○ with its reason on the row.
2. ~~**Out-of-date datasets**, and **find datasets with a given column**.~~ — **both done (§352, §353)**, and struck in halves as each landed per §216. They shared a line and nothing else: one compares build times across edges, the other counts column names across schemas, and neither needed the other. "Cheap" held for both — no migration between them, because `datasets.table_schema` and `dataset_versions.created_at` were already on the rows the graph reads. **The first half is done (§352)** and struck here rather than at the end of the pair, per §216 — a line goes stale in the commit that finishes *part* of it, which is how three entries of `ontology.md`'s list came to describe work already shipped. The two are independent despite sharing a line: one compares build times across edges, the other searches schemas, and neither needs the other. "Cheap" held for this half — no migration, one walk over edges the graph already had.
3. ~~**Search & Browse helper**, node expansion, drag-select.~~ — **done (§354, §355, §356)**, struck in thirds per §216 as each landed. It came first because it was the one of the three that was already owed: §353 shipped p.55's histogram with no selection to compute it over. The other two share a prerequisite this one did not need, and it is worth writing down before starting them — **both are about a graph that is less than everything.** p.7's arrows expand "related resources" onto the graph and p.8's helper adds resources to it, and neither means anything here, because `project_graph` draws the whole project already (`focus` narrows to a connected component, which is everything reachable). So they are not two features to copy but one question to answer first: whether this graph should start narrow. p.8 asks it too, warning that recursively adding all tables in all subfolders "may effect the graph's performance". **§355 answered it: no.** `project_graph` has no node cap (checked — the only `LIMIT`s in `pipeline.py` are the per-row `ORDER BY ... LIMIT 1` subselects) and `focus` narrows to a connected component, which is everything reachable; but the scope of this graph is a *project*, and a project is the unit a person works in, where Foundry's is an enterprise. So p.8's warning describes a problem in a different shape, and its mitigation — start narrow, add what you need — would be §214's control that cannot work: a Search & Browse that adds resources already drawn. **Both rows translate to the selection instead**, which is why the prerequisite they turned out to share was §354's rather than a narrow graph. p.8's helper does two things and only one of them is adding: *find* carries over as-is (locating a node by name on a forty-node graph is real whether or not it is drawn), and *"add all search results"* becomes *select every match*, which is what feeds p.55's histogram; the Advanced tab's filters become filter-by-kind. That is what §356 built, and the row carries the rest. **The three turned out to be one feature in three parts**, which is not what this line said: drag-select, expansion and search all answer "which nodes am I asking about", and each one is worth more because the other two exist — search for a source, expand downstream from it, read the columns of everything it feeds. §216's note about build-order estimates was right again here, though in the other direction than usual: the line over-counted the features and under-counted how much they depended on one another. **The sweep scored 17/0** — 8 on the walk, 7 on the wiring, and 2 more written against claims in the code that nothing checked (a *Update selection* offered before anything is highlighted, which would narrow to the empty set and silently empty the graph; and one that cleared the highlight it had just acted on). One catch is worth naming for what it is: the mutant that lets the frontier grow instead of narrowing is caught by the run **never finishing** rather than by an assertion, because `hops` is a distance and a frontier that only grows never empties — finite hops are unaffected, so `Infinity` is the only case that can show it. And the harness itself cost a restore: a sweep that times out leaves its mutant applied, and `git checkout --` then reverts the *unit's own new code* along with it. Sweeps restore in a `finally` now, and a timeout is scored as a catch.
4. ~~**Branch from a historical transaction** (Dataset Preview) — the missing half of §56.~~ — **done (§357)**, and the line was wrong twice over, which §216 caught by opening what it cited. It was not "the missing half of §56": §56 is the dataset application and time travel is §96, and the branch action itself had existed since migration 0025 — what was missing was p.4's *placement*, on the transaction rather than in a dropdown that asks again. Checking also turned up a defect nobody had recorded: forking a version whose bytes are gone raised `FileNotFoundError`, the same 500 §56 fixed on the read path and never on this one. **Cheap held, for once by being cheaper than the line said** — no migration, no new endpoint, one refusal split into three and a button moved to where p.4 puts it.
5. ~~**Build logs** in the job detail view, and the History summary.~~ — **done (§358, §359)**, struck in halves per §216; the Summary view was the other half and shared nothing with it — one is a capture pipeline across two runtimes, the other an aggregate over a window. **This one was bigger than the line, in the direction §216 keeps warning about**, and for a reason worth recording: customer Python runs in *two* places — a local subprocess in development and a container in production — so a log kept in one of them would have been green in the suite and missing in every deployment. That is §292's exact failure, in the same two files, and avoiding it a second time cost more than the feature did. The sweep proved it worth the cost: the container half's three mutants **all survived** the first pass, because nothing tested the path that only runs in production.
6. ~~**Save and share a graph.**~~ — **done (§360)**, and the line's estimate was right for once, for a reason worth recording: **most of the answer already existed and the work was refusing to build it again.** The §191 check found db 0040's saved searches, which had already decided scope, sharing, storage and validation for a saved thing, and `CopyLinkButton`, which had already decided what a share link is. So the two halves of p.12 came down to one table that copies 0040's shape with its scope changed and its reason written down, and to *making the address bar true* — which turned out to be a bug fix as much as a feature, since this page lost its whole state on reload. The one place it grew was the sweep, where three stages were needed because the view has three homes (what the server will store, what the URL carries, what the graph draws) and a mutant in one is invisible to the other two. **What this line could not have said** is that finishing it would delete code: a redundant URL write and an entire unused route, both found by asking what a mutant could change without anything noticing.
7. ~~**Roll back a dataset**, then roll back a pipeline.~~ — **the first half is done (§361); the second is withdrawn**, and the withdrawal is the finding. §216 says open what a line cites before building on it, and p.64 turned out to mark *Roll back a pipeline* as planned deprecation — "unavailable for use after November 30th", with Foundry recommending the dataset feature instead. A line written from a table of contents cannot know that; the page says it in the first screen. Struck in halves per §216, with the pipeline half recorded on its own row as ○ *by decision* rather than left looking unstarted. **The half that was built cost less than the line implies**, for a reason §360 also ran into: the ground was already prepared. Every version's bytes have always lived at their own key with nothing overwritten, so a rollback copies nothing; `produced_by_kind`/`produced_by_id` already meant "what produced this version", so there was no migration; and two restore features (db 0024, db 0028) had already settled that a restore appends rather than rewinds. **What it did add was a warning to *not* copy.** p.76 asks the reader to acknowledge that a rollback cannot easily be undone, which is not true here — nothing is deleted — and a dialog that said it would have been frightening somebody out of a reversible act.
8. ~~CSV parsing options and Edit schema.~~ — **done (§362)**, and the line under-counted it in the way §216 keeps finding: the interesting part was not the options but **where they act**. p.24 puts the Edit Schema UI on the preview tab and has it rehearse the parse before you keep it, which means re-reading a file that was uploaded some time ago — and that turned out to be possible only because of something nobody had noticed: the upload route has been storing every uploaded file verbatim since the day it was written, under a key whose name it then threw away. So a whole feature came down to one column (db 0090) plus the parser work. **Two things were withdrawn on the way**, both by the same test: an `escape` option and Foundry's `recordDelimiter`, neither of which could be made to change DuckDB's answer on any file that could be constructed. A control a mutant cannot kill is a control that cannot be shown to work, and shipping it would have been worse than leaving it out.

---

## 4. Acceptance tests

- **Impact analysis** — a graph containing a dataset shows the object types backed by it; deleting the mapping removes them from the graph.
- **Out-of-date** — a dataset whose upstream rebuilt is marked stale; rebuilding clears the mark.
- **Column search** — a column present in three datasets returns exactly those three.
- **Branch from transaction** — a branch created from transaction *N* reads the data as of *N*, not as of head.
- **Branch fallback** — a graph on branch `feature` shows resources that exist only on `main`, in the documented fallback order.
- **Rollback** — rolling a dataset back to transaction *N* makes head read as *N*, and the rollback itself appears in history.
