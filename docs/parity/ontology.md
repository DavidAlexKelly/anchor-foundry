# Ontology — parity specification

**Covers:** Ontology Manager, Object Explorer, Object Views, Action Types.

**Sources:** `foundry_ontology.pdf` (172 pp), `foundry_ontology-manager.pdf` (74 pp), `foundry_object-link-types.pdf` (274 pp), `foundry_action-types.pdf` (174 pp), `foundry_object-views.pdf` (140 pp). Citations name the file: `(object-link-types p.127)`.
>
> Section 2.6 also cites `foundry_workshop.pdf` (718 pp), which belongs to `workshop.md`: time series properties are defined in the Ontology and consumed by Workshop's widgets, and the list of *which* widgets consume them is written down only there. Cited as `workshop p.582` for that reason, and named here so the citation resolves — §302 found it unqualified, pointing past the end of every source this document had.

**Today:** `apps/api/src/services/ontology.py`, `instances.py`, `object_sets.py`, `actions.py`; `components/applications/object-type-app.tsx`; Object Explorer at §58; OpenSearch instance store at §16.

> **Source gap.** Object Explorer has **no dedicated PDF** in `docs/pal/`. Section 3 below is reconstructed from the standard Object View documentation, the application reference (`getting-started` p.48), and scattered mentions. It is the least well-sourced section in this set, and the one most likely to need correction against the real product.

This is the foundation the rest of the parity work stands on. Workshop's object widgets, Object Views, and function-backed actions all resolve to what is here.

---

## 1. Object types and properties

### 1.1 Property base types (`object-link-types` p.127)

Ours: `string`, `integer`, `float`, `boolean`, `date`, `timestamp`, `geopoint`, `json`, `attachment`.

| Foundry base type | Status | Notes |
|---|---|---|
| String, Integer, Double/Float, Boolean, Date, Timestamp | ✅ | |
| **Geopoint** | ✅ | §20 |
| **Attachment** — files on objects, for use with functions | ◑ | we have the type; no file storage behind it |
| **Geoshape** | ○ | polygons and lines, not just points |
| **Time series** | ◑ | §148, §149 — the type, the `object_type_series` mapping (db 0047), the points read, and the chart on the standard Object View. §151 added **Workshop**'s time series set variables (`workshop.md` §3.2) and the Chart XY input that reads one. What is left is the other three widgets workshop p.582 names — Map, Metric Card, Object Table — and workshop p.583's time series *transforms*; geotemporal series is the same mechanism with a geopoint value column and is ○ |
| **Geotemporal series** | ○ | position over time; renders on a Map in standard Object Views |
| **Media reference** | ○ | points at an item in a media set: `mimeType` plus a reference triple of media-set / view / item RIDs (p.128). **Decision 0009 declines to add the type**: a shape promising a media set with none behind it is a contract nobody honours. Media that is already stored renders (§147); a media *set* waits for a consumer that needs a collection |
| **Struct** — schema-based properties with multiple fields | ◑ §245 | (`object-link-types` p.149–160, and the modelling argument in `ontology` p.58–59). The type, its declaration and both write paths. **The schema is the whole difference from `json`**, which has accepted the same *values* since db 0003 — so a struct is not a new value, it is a claim about one, and p.149's four constraints are refusals rather than conventions: depth of one with its **own** message (somebody who has read p.58's address example will try nesting), at least one field, a field-type list *narrower* than this platform's property types, and unique named fields in a declared order. **The fields are a column on the property row, not a child table**, and 0028 is the reason: an edit deletes every `object_type_properties` row for the type and re-inserts it, so a child table keyed on the property id would have a struct's schema emptied by renaming a *neighbouring* property. Three things decline to carry one and say so instead of half-working — a **shared property** (p.178 shares one definition and this table has no column for the fields), an **action parameter** (p.150 lists Actions, so this is a gap: coercing a struct needs the declaration, and the `{name: type}` map every write path reads is built in eight places, so widening it is its own unit), and **field search or sort on either store** — that last one is Foundry's own position rather than a shortcut, since p.150 ships struct support with "struct field search is under development" and warns that array-of-struct queries match fields independently rather than within one entry. **§246 added p.152–158's editor**, so the type is offered in the property dropdown and a struct is declarable by a person: a Struct fields section with add, edit and remove, and p.158's rename warning translated into this platform's mechanism — there is no RID here, and what a rename actually costs is that every stored value keeps the old key until the next sync, which is the sentence the dialog shows because it names the fix. A struct also **renders as its fields rather than as JSON**, which needs the declaration for the labels *and* for the order: `jsonb` reorders an object's keys on the way into storage, so the value itself no longer remembers what p.154's author chose. ○ for p.153's **Backing column**, p.155's **per-field column mapping** and p.160's **Automap all** — one feature in three places, and they say where a field's value comes from; this platform maps a struct the way p.149's own first sentence describes, from one "struct type dataset column", which `column_mappings` already expresses. Also ○: p.59's **main fields** and **reducers**, which are about arrays of structs, a property type this platform does not have |
| **Vector** — for semantic search | ○ | out of scope unless semantic search is |
| Cipher text | — | out of scope (Cipher is out of scope) |
| **Arrays of any base type** except Vector and Time series | ○ | |

Ours has `json`, which Foundry does not — Foundry's equivalent is Struct, which is typed. **Recommendation:** keep `json` as an escape hatch, add Struct as the typed path, and do not let `json` become the way people model structured data. **Acted on in §245**, and the recommendation held up under the code: the two types share a coercion path and differ only in whether anything is checked, so `json` stays exactly as unconstrained as it was and `struct` is the same value with a declaration in front of it.

### 1.2 Property configuration

| Feature | Status | Source |
|---|---|---|
| Display name, description, API name | ✅ | |
| **Visibility** — normal, prominent, hidden | ✅ | (`object-link-types` p.111); drives standard Object View layout (`object-views` p.10), Explorer columns, and the Linked objects rows (§145 — one shared rule, because the third surface is where a second copy of it leaked) |
| **Value formatting** | ◑ | §157 — p.94-101. Numeric (currency, unit, percentage, prefix/suffix, grouping, notation, and p.98's five digit options) and date/time (p.99's six styles, and p.100's timezone: a named zone or the reader's own). Applied in the browser, because p.100's "the application user's current timezone" is not something a server knows — and because formatting on the way out would make filters, actions and exports read `"$100K"` where they used to read `100000`. **What is missing is p.95's three lookup formatters** — Foundry ID, resource RID, artifact GID — each of which turns an ID into a name by *asking something*, so none is a transformation of the value in hand; and p.97's Fixed Values, which is a value-to-label map for the same reason |
| **Conditional formatting** | ◑ | §158 — p.102-109. An *ordered* list of rules per property, first match wins, with p.105's always-true fallback (refused unless it is last, because everything after it is unreachable). Standard rules across p.105 label C's comparisons — string with p.105 label D's four operators, numeric range and exact, boolean, is-null — reading either the property being painted or **another one** (label B), against a constant or another property's value (label E), invertible (label F). Colour, background and alignment. **It composes with value formatting rather than competing**: the rule compares the raw stored value and the formatter decides the text, which is what lets one property show "$100K" and be coloured by a 50000 threshold. Missing: p.105's **Math rule**, which runs arithmetic over properties and is an expression language rather than a comparison; p.107's **Copy rules** dialog; and Blueprint's named colours and intents, since there is no Blueprint here — colours are hex |
| **Required properties** | ✅ | §154 — p.116. The flag has existed since migration 0003 and meant nothing; now **actions refuse** a write that empties one and **sync reports** the rows that do not comply rather than refusing to index them, which is p.116's own split |
| **Edit-only properties** | ◑ | §160 — p.113-115. A property with no column in any backing dataset: written by an action straight to the instance, **preserved across syncs** rather than deleted by one, and refused as a mapping target (p.114's own flow is to untoggle first). A stored flag, not "absent from every mapping" — the two are the same state and different intentions, and telling them apart is what schema drift detection exists for. The exception to the write-back rule is deliberately narrow: a `modify_object` rule on the action's own subject. A **link** property still needs a column (a link is a join over stored data), and `create_object` still refuses one, because a creation's dataset row is how the object comes to exist and there is no instance yet to write the value to. Not built: p.113's permissioning of an edit-only property to one of the backing datasets |
| Mandatory control properties | ○ | p.121-126. **Declined for now, and the reason is structural rather than effort.** These are row-level access controls enforced by *markings*, *organizations* and **restricted views** — p.124 is explicit that "the mandatory controls are enforced by backing the object type with a restricted view which has a policy that requires users to satisfy the markings in the mapped column". This platform has organisations but neither markings nor restricted views, so the enforcement mechanism the whole feature rests on is absent. A flag that looks like access control and enforces nothing is the worst kind of gap — worse than an absent feature, because somebody will rely on it. Same reasoning as decision 0009 declined the media reference type |
| **Property reducers** | ○ | TOC §18 |
| **Derived properties** — calculated at runtime from other properties or links | ◑ | §161 declares one, §162 answers it, §163 draws one. A chain of up to three link types (p.147), an aggregation (p.145) and the property at the far end (p.146), built a hop at a time — each step offering only the links that exist from where the chain stands (p.145) — evaluated when an object is read and never stored (p.143). The chain is an object set rooted at the one object, so §155's traversal answers it. **Still ◑ for one reason, and it is a real one: four of p.145's nine aggregations are refused** — `sum`/`avg`/`min`/`max` on the untyped-index blocker (§52, §74, §83, §86; this is the fifth) and `approx_cardinality` because OpenSearch approximates where Postgres is exact. p.143's own first example, a department's average employee salary, is among them. Also single-object reads only: a derived *column* in a table needs the aggregation pushed into the index, which is the same blocked work |
| **Shared properties** — one definition used across object types | ◑ | §164 — p.178-191. One definition in a workspace, attached to a property on any number of object types, with `display_name`, `description`, `visibility` and `value_format` **resolved at read time** rather than copied at attach time — which is p.178's own reason to exist ("update … metadata in one place instead of on each object type") and the half a copy would silently break. The property keeps its own id and api_name (p.188), so `began_on` on Employee and `start_date` on Contractor can be the same shared property. p.181's base-type match is enforced; p.188's "direct edits … will be disabled" is a refusal on the wire, because a value accepted and discarded is somebody's edit vanishing with nothing to explain it; **attaching is exempt from it**, since choosing a shared property is choosing its metadata. p.185's delete **reverts rather than cascades** (`ON DELETE SET NULL`), and the property keeps its last inherited metadata. p.191's Usage names the object type *and* the property. Missing: **type classes** and **render hints**, absent rather than stubbed — nothing in this platform reads a type class, and reindex tuning is not something this instance store exposes; and the **Ontology Manager surface** (p.180's shared property page, p.187's dropdown, p.178's globe), built in §165: p.180's page, p.181's creation modal, p.187's dropdown offering only the shared properties whose base type matches, p.188's Detach and its two disabled controls, p.191's Usage, and p.178's globe. **The row is ◑ for the two absences above, not for the surface.** One deliberate divergence: a shared property **in use** cannot change its base type — Foundry lists it as editable and does not say what happens to the object types using it, and a silent cascading retype is the exact change `type_impact` exists to make somebody acknowledge |
| **Value types** — reusable constrained types, versioned | ◑ | §168 — p.222-234. A semantic wrapper around a base type carrying a **constraint**: p.222's own example, an `email` value type whose regex means every property using it is understood to hold an address. The constraint-sibling of shared properties (a shared property shares *metadata*, a value type shares a *rule*), attachable in both of p.227's places — an object type property and a shared property, with a property inheriting its shared property's when it has not chosen one. p.229's split is the schema: name/description are editable, **base type and constraint are immutable and a change appends a version**, and p.230's propagation is automatic because a property references the value type and the current version is the highest-numbered one. Constraints built: enum (with p.233's case-insensitive option), range (numbers, temporals, and a string's *length*), regex (with p.233's substring option), uuid. **Deliberate divergence on enforcement**: p.227 has a failing value make the object type "fail to index", which would take a whole type off every screen for one bad row — this platform reports on sync and refuses on action, which is p.116's own split as applied in §154. Missing: p.233's `rid` (Foundry-specific; our resource ids are UUIDs, so `uuid` is the same check under one name), array and struct constraints (neither base type exists, §1.1), Foundry's separate **Value Types Manager** and its cross-project import (p.232 — one ontology per workspace here), and **deprecation** (p.229), which is §1.3's Status row rather than a second flag. `api_name` is immutable here though Foundry allows renaming, for `object_types.api_name`'s reason. §169 built the **Ontology Manager surface**: p.224's create form with the constraint editor offering only the kinds the base type allows (p.233), a range that says whether it bounds a *value* or a string's *length*, p.227's dropdown on a property, p.229's constraint change as an explicit "Change rule" that appends a version beside the previous ones, and the usage list across both attachment points. A property inheriting its shared property's value type says so and shows `↑` rather than `•`, since the two are different states and only one is this property's own choice |
| Title property / primary key | ✅ | |

### 1.3 Object type configuration

| Feature | Status |
|---|---|
| Backing dataset mapping | ✅ |
| Icon and colour | ◑ |
| Groups / categories | ◑ | §172 — p.261-263. "A classification primitive that help users better search and explore their ontology" (p.261), and nothing more than that: a group carries no schema, so grouping an object type does not change it, and deleting a group deletes the classification rather than anything in it. Both of p.261's directions exist — a group's membership, and "Edit groups" from the object type — writing through one path so the two screens cannot disagree. p.262's three appearances: findable in ontology search, a column on every row of the object type listing, and a `group_id` filter on it. **The rule that decides the implementation is p.263's**, which records Foundry *changing* it: a group used to be non-discoverable when all its members were, and now "all groups will now be discoverable to any user that can view the ontology … to increase clarity and transparency in governance". So a group's visibility is a fact about the group, never derived from its members — one RLS policy on `workspace_id` and no join, which is also the shape this repo's own recorded RLS bug class (0008/0009/0015) argues for. The membership row carries a denormalised `workspace_id` for the same reason, pinned by composite foreign keys that additionally spell p.192's boundary in SQL: a group cannot contain an object type from another workspace. Workspace-scoped rather than project-scoped (p.263 says project) because a workspace is this platform's ontology, as for shared properties and value types. §173 built the **Ontology Manager surface**: p.261's groups menu with a whole-membership picker, p.261's "Edit groups" on the object type, p.262's chips on each row, its `group_id` filter above the table, and the group kind in search. Two decisions worth naming. **An empty group is drawn like any other** — the count reads "0 object types" rather than going quiet — because every group is empty for the seconds after it is created, and a listing that hid it would fail p.263 on the very first use. And **the edit dialog writes the membership only when it changed**: the groups are a second resource with a second verb, so a dialog that PUT them on every save would reintroduce the carry-through failure one layer up, silently un-grouping a type whenever two people had the page open. Missing: p.263's legacy tag migration and type-class metadata (there was never a legacy system here to migrate from), and the Object Explorer home page's group sections |
| **Interfaces** — shared property and link contracts across object types | ◑ §255 `[?]` | **The second-source gap §168 named is real and this row is the decision it asked for**: *"needs the missing pages, or an explicit decision to design it from the fragments and mark what was guessed"*. Taken, and marked. `docs/pal/` still has no Interfaces chapter — p.4 and p.8 define one and point at a page the export does not carry, and every other mention is the Gaia/Gotham integration (p.51–73), which is out of scope. What the fragments *do* settle is the model: a shared shape of properties, links and actions (`ontology` p.60); many object types implementing one and interfaces extending "any number" of others (`object-link-types` p.53); a worked example with three implementing types (p.61); and an implementation that **maps** the interface's property onto the type's own (p.66) rather than matching names. **Built:** the interface, its properties, extension as a graph, implementation with a mapping, and the refusals that make it a promise rather than a label — a required property with nothing mapped to it, a mapping to a property the type does not have, a base type that does not match (p.181's shared-property rule one resource over), a circle of extensions, and two ancestors declaring one name with different types. **Guessed, and named here because the row asked:** that an interface is named like an object type (inferred from p.60's `Inspectable`, `SchedulableResource`); that a property may be *optional* (nothing in the source says so — it is here because p.62's "design interfaces around capabilities" needs a capability with one mandatory field and two optional ones); that an interface carries a **status** like every other ontology resource; and that deleting one in use is **refused**, which is deliberately unlike p.185's shared property reverting its users — a shared property gives a type metadata it can live without, an interface is a claim other resources are written against. **Findable** as of §252: the header search covers it, reporting how many object types implement it — §167's argument about shared properties, which applies harder here, since two interfaces with the same shape are two sets of implementations to keep in step. **The Ontology Manager surface is §253**: a panel beside shared properties and value types (the same decision §165 and §168 made, for the same reason — one ontology per workspace, nothing to import across), with two dialogs rather than one, because what a shape *is* belongs to whoever wrote it and what a type *claims* belongs to whoever owns the type. The implement dialog makes three of the server's four refusals unreachable rather than restating them: a row per declared property, a select offering the type's own properties, and only those whose base type matches — leaving one rule stated on both sides, and that is the one somebody needs answered before they click. §253 also found and closed a gap §251 left: p.256's status gate applied to every other ontology resource and not to this one, so an interface's status was a label rather than a state. **And §254 targets it directly**, which is p.61's own phrase and what turned this row from metadata into a feature: `POST /interfaces/{id}/evaluate` reads every object of every implementing type, filtered in the interface's vocabulary and returned in it — a Vehicle's `last_checked` and a Facility's `surveyed_on` both answering `last_inspection_date`. **A fan-out over the existing evaluator rather than a wider object set**: one `evaluate_object_set` per type and a merge on a key each row carries, so there is no store code and the two stores cannot disagree about what an interface set means. A *property* sort is refused (ordering by one means comparing in its declared type, §221, which each implementing type would answer differently) and paging is bounded at one store page and refused past rather than clamped. A type that answers nothing to a filtered optional property is **skipped**, and the answer reports which types were read — "no Facility matched" and "Facility was never consulted" are different facts. **§255 is the screen**, reached from an interface's implementation count: one table whose headings are the interface's properties and whose rows say which type each object is, with paging that stops where the server refuses and says why. **○:** links and actions on an interface (p.60 names both; a link has two ends and an action has parameters, so each is its own question), `struct` as an interface property type (the promise is its *fields* and `check_implementation` compares base types, so it is held out of the dropdown rather than accepted and unenforced), and filtering an interface set from the browser — the API takes filters, the dialog does not offer them yet |
| **Status** — promoted / active / experimental / deprecated / example | ◑ | §170 — p.253-259. All five of p.254's values on object types, properties and link types, defaulting to `experimental` (p.256). **The refusals are the feature**: an `active` or `promoted` resource cannot be deleted, and the message names the way through. p.256/p.258's propagation demotes a type's properties with it and **never promotes them** — p.258 makes that an option, not a consequence, so a half-built field is not declared production-ready by somebody finishing the type around it. p.257's table is implemented as a *cap* rather than a warning: a link type is stored at the lowest of its own declaration, its two object types and its join properties, so the invalid state p.257's troubleshooting section describes is unreachable rather than detected. **§170 shipped that cap as a validation and missed the event, and §176 fixed it**: the cap was computed only when the *link* was edited, so demoting an object type left an `active` link hanging off an `experimental` one — reachable in two API calls, and the exact state this row previously claimed was unreachable. `recap_link_types` now re-applies it to every link touching a type whose status or join columns just changed. It lowers only, which is p.257's own asymmetry: it says a link "will automatically be changed" when a dependency drops and says nothing about restoring one when a dependency recovers. p.254's deprecation note (why, by when, what replaces it) is refused on anything not deprecated and cleared when a resource stops being deprecated. §171 built the **Ontology Manager surface**: p.256's dropdown offering only what the kind allows (p.255 makes `promoted` object-types-only, so offering it elsewhere is offering a save that fails), p.253's badge in the listing — but *not* on `experimental`, which is the default and would therefore label every row of a new ontology, a Delete button disabled with the server's own sentence as its reason, p.254's note offered only where the server accepts it, and **a warning naming the properties a demotion is about to take with it**, on screen while the choice is still a choice, since p.256's propagation is otherwise invisible until it has already run. §174 closed **action type statuses** — p.253 names actions among the kinds that have one, and the column had sat there since §170 with nothing behind it. All four values (p.255 excludes `promoted` by name), p.256's delete refusal, p.254's note, and "omitted means unchanged" on the only endpoint that writes one. **No cap from the object type**: p.257's table is about link types and its own reasoning is specific to them, so extending it to actions would be inventing a rule — which is precisely why deleting an object type now *refuses* while an `active` action hangs off it, since `action_types.object_type_id` cascades and would otherwise delete a protected resource without demoting it. §175 built **p.255's permission and visibility sentences**. Only a workspace admin may *apply* `promoted` — a workspace is this platform's ontology (db 0003) and `workspace_role` already had a tier above editor, so p.255's "Ontology Owner role on the ontology level" is a floor rather than a new permission system. **Foundry's own Ontology-roles chapter is marked legacy** (`ontology-manager` p.43: "no longer the most up-to-date method… can now be permissioned using the Compass filesystem"), so a per-resource role registry would have replicated the model Palantir is migrating off; the divergence to hold in mind is that our admin is workspace-wide where theirs is per resource, which is stricter rather than looser. The gate is on the **transition**, not the value: the editor sends the whole definition on every save, so gating the value would lock every editor out of every promoted type. Promotion also sets the type's visibility to `prominent` (new column, db 0057) and **never lowers it again** — p.255 says what promoting does and nothing about demoting undoing it. Missing: p.255's second sentence, a proposal others may submit for an owner's approval (it needs a review surface for ontology changes, which is a feature rather than a rule). **§177 built p.258's bulk edit**, all three sentences: statuses across selected object types, statuses across one type's properties, and the **option** to apply a type's status to all its properties — an option rather than a consequence, which is what keeps §170's lower-only asymmetry intact. The bulk path runs the same pure rules the single-type path does (p.255's role and visibility, p.256's propagation, p.257's link re-cap, and a version per type), because a bulk edit is a way round every rule it forgets. Bulk property statuses are **capped at the object type's own status**: p.256's propagation runs on every save of the type, so a property raised above it would be silently demoted by the next unrelated edit — the carry-through failure in a new place, refused rather than created. One refusal fails the whole request; the caller chose those types together, and a per-property status control — a property's status is carried through and propagated but is not yet individually editable on the type form |
| Indexing / reindexing state and errors surfaced per object type | ✅ §313 | **The same mechanism as p.29's issue column below**, which is how this was built: two rows of this document turned out to be one thing seen from the type and from the Ontology Manager. It needed no storage — `object_type_sources` has carried `sync_status`, `last_synced_at` and `last_error` since db 0003 and nothing had ever shown them |

---

## 2. Link types (`object-link-types` p.192)

> "A link type is the schema definition of a relationship between two object types… A link type is **bidirectional**: it always has two sides, one for each of the two object types it relates. Each side of a link type can be traversed independently and has its own display name."

| Feature | Status | Notes |
|---|---|---|
| Cardinality one-to-one, one-to-many, many-to-many | ✅ | |
| Link traversal from an instance | ✅ | §18 |
| **Per-side display names** | ✅ | stored, resolved and rendered (`STATUS.md` §123); `side_name` comes back already resolved against the link's own name, so a caller never has to know which end it is on |
| **Self-links** — a link type between an object type and itself | ✅ | **§2 was wrong to call this absent.** `link_types_for_type` already returned a self-link twice, once per direction, deliberately. What was missing was two *names*, so the directions could be told apart — which is the row above |
| Independent traversal of each side | ◑ | |
| Link traversal inside an object set definition | ✅ | §155 — a set can be the far side of a link (`via`), in either direction, capped at three hops and 1000 join values. A hop compiles to an `in` filter, so it needed no new store capability; the primary key became filterable on both stores, which is what makes the second direction possible |

Per-side display names and self-links are both small and both currently impossible to express. They should go together.

---

## 3. Object Explorer

*Reconstructed — see the source gap note above.*

| Feature | Status | Notes |
|---|---|---|
| Workspace-wide object browsing | ✅ | §17 |
| Search across object types | ✅ | §63 |
| Filter by property | ✅ | |
| Link traversal | ✅ | §18 |
| Open an instance into its Object View | ✅ | §122, §144 — the generated view, or a configured module if the type has one |
| **Favourite an individual object** — star next to its title, added to the sidebar | ✅ §312 | `getting-started` p.34; db 0074, `apps/api/tests/test_object_favourites.py`, `e2e/test_object_favourites.py`. **§309 had to come first**: p.34 calls these shortcuts, a shortcut needs somewhere to point, and until an object had a URL there was nowhere — so the favourite is a link and the two rows are one mechanism. The star sits on the object view rather than literally beside the title, because the title lives inside the *standard* view and a configured view has no title of ours at all; putting it in either rendering would give it to one kind of object and not the other. **p.34's sidebar is the Explorer's aside**, beside Saved searches: one is a shortcut back to a question and the other back to an answer, and Foundry's sidebar is platform-wide where ours is not. Per user, enforced by the row policy. The label is **stored**, because resolving ten titles live is ten reads against the instance store before a sidebar of ten can draw — and refreshed whenever the object is starred again, which is the one moment the current name is in hand. A full list **refuses rather than evicting**, the opposite of §306's history and for the opposite reason: a history is a record of what you did, a favourite *is* a decision. A shortcut outlives the object it names, because an instance is not a row this schema can reference — so following one lands on §309's dead-link message rather than the row quietly disappearing |
| **Direct object edit from the Explorer** | ◑ §324 | **The citation was wrong, and §216's rule found it**: this row cited `ontology-manager` p.32, which does not describe the feature at all — it names "direct Object Explorer edit" in a list of five things that count as a *write*. The specification is `action-types` p.135: "Inline edits are available in both Workshop and Object Explorer… Inline edits allow users to quickly edit values of an object in the **Object Explorer results view**." So this is not a new write path but the second surface for §238's action-backed inline edits, which is also why it needs no migration: eligibility is computed on the server, the batch route enforces p.138's all-or-nothing, and p.241's automatic mapping already matches a parameter to a column of the same name. **The server half is built**: p.136-137's requirements are the action type's and were being mixed with `workshop` p.241's advice about a *table's* columns — p.137 lists visibility among the requirements only to say it is allowed ("can be set; however, they will be ignored if the inline edit is used in Object Explorer and Object Views"), so §238 refusing an action for a hidden parameter was stricter than either page and took otherwise eligible actions out of inline editing entirely. It is now `hidden_inline_parameters`: a column not offered rather than an action not used, which is safe because p.135 makes every parameter default to the object's existing value, so one no column offers is submitted unchanged . **The surface is built**: an Edit button over the results, p.241's automatic mapping putting an editor on every column whose name matches a parameter, staged edits with p.242's row cap and per-row Undo, and one submission through the batch route — all of it reusing §239's machinery rather than a second copy. **The obstacle worth recording is that the Explorer is workspace-scoped and a write is not**: an instance comes from a mapping, a mapping names a dataset, and a dataset lives in a project, so a type mapped from two datasets in two projects genuinely has two destinations. `editing-projects` answers where a write would land and the Explorer **names both and refuses** rather than picking one — an edit that silently chose would write to a dataset the reader never named. Five states put no editors on the table (several types, a viewer, no eligible action, no dataset, two datasets) and they look identical, so each says which it is and a test asserts the five sentences differ — a function answering "unavailable" for every state would satisfy each check for that state on its own. Writes are labelled `explorer`, which makes p.32's fifth write source real: until §324 that column was a heading over a number that could not move. ○ for p.136's per-property **Inline edit** binding in the Properties tab — this build maps parameters to columns by name (p.241) rather than binding one action type per property, which is the same information from the other side but not the same configuration surface . **The sweep scored 25/6**, and the sharpest survivor is worth recording: a submission sending *every visible row* rather than the staged ones passed every assertion, because p.135 makes each parameter default to the object's existing value — so an untouched row submitted with no values writes its own value back and "the row nobody edited is unchanged" stays true. What changes is the record: a run is opened for an object nobody edited, and p.242's cap is counted against rows never staged. The count of runs in the batch is what tells them apart. Two more were the same shape as §320's and §323's: the `if ok:` guard around the write count could not be reached, because every refusal in the suite raises *before* the write is attempted — it needs a failure in the dataset write, which answers 200 with `ok: false`. **One survivor could not be fixed with a better test at all**: `editing_projects`' `ORDER BY` cannot be distinguished in a single run, since two rows arrive in whatever order the plan produces and that coincides with alphabetical about half the time. A check that is right 50% of the time is worse than none, so the order a reader *sees* moved into `explorer-edit.ts` where it is exact, and the SQL clause is now the belt to that braces. **Two of the six were the harness being wrong**, not the tests: a mutant on `inline-edit.ts` was judged by a vitest layer that only ran `explorer-edit.test.ts`, and `<button hidden className="btn">` hides nothing because `.btn` sets `display: inline-block` — which `globals.css` already documents |
| Save and share a search | ✅ §308 | **A third row that said ○ for work that exists** (§307's audit; the two others were in `code-repositories.md`). It is built: db 0040, `apps/api/src/services/object_searches.py`, and the Save this search dialog in `apps/web/src/components/object-explorer.tsx`, with 18 tests in `apps/api/tests/test_saved_searches.py` — including the one the schema comment argues for, that a saved search stores the question and never the answer. Its empty Notes cell is the tell: nobody revisited it. **✅ as of §308**, which wrote the missing half: `e2e/test_saved_searches.py` types a search, saves it, *clears the box*, and opens it again — the clear is what makes the assertion mean something, since without it the test passes against a saved search that stores nothing at all. Sharing between people is deliberately not claimed there: the browser suite's `page` and `api` fixtures carry the same token, so it is checked in the API suite, which saves as the editor and reads back as the viewer. The section that built it is not recoverable — history is squashed per pull request — which is the same evidence §302 used when it declined to check `§NNN` citations at all |

---

## 4. Object Views (`object-views`)

**The highest value per unit of work in the whole parity set**, and cheaper than it looks: configured Object Views are "fully customizable representations **built using Workshop**" (p.2). We have that engine.

### 4.1 Standard Object Views — generated, not configured

"When you create and configure an object type in your Ontology, Foundry automatically creates a standard Object View" (p.10). **Built — `STATUS.md` §122**, less the two renderings that need property types we do not have.

| Feature | Status | Notes |
|---|---|---|
| Auto-generated for every object type | ✅ | no configuration, no saved document, nothing to publish |
| **Prominent properties surfaced at the top** | ✅ | the payoff for property visibility (§1.2) |
| — geohash / geoshape / geotemporal → rendered on a Map | ✅ | ours is geopoint; one point is still a map |
| — everything else prominent → large card above a table of the rest | ✅ | |
| — media reference → dedicated media viewer | ◑ | §147 — an attachment holding an image, video or audio renders inline; the *media set* Foundry references is genuinely absent (decision 0009 declines to build one), so the property type stays ○ |
| — time series → interactive chart | ✅ | §149 — a prominent `time_series` property draws its line from the points in the dataset behind it (decision 0009); bucketed by day, because a card is not an analysis surface |
| Normal properties in a regular table; hidden properties not shown | ✅ | |
| **Linked objects component** — grouped by link type | ✅ | all four of p.11's capabilities. The groups are there (§18) and sit *inside* the view rather than beside it; a linked object's properties preview inline without navigating (§145), typed, prominent first, hidden absent; **§159** adds the other two — opening a subset in a new tab, which needed no new query because a link is a derived join (db 0027) and the subset is therefore a filter the Explorer already speaks, primary-key sentinel included; and the side panel, which holds one selected object beside the view and clears itself on a hop |

### 4.2 Configured Object Views

| Feature | Status | Notes |
|---|---|---|
| Built in Workshop, becomes the default view once created | ✅ | §144 — a published module is nominated in the Ontology Manager and stands in for the generated view. The object arrives in the module's `single_object` variable, which is the whole binding |
| **Users can always switch back to the standard view** | ✅ | §144 — a control on the view itself. **This row used to add "and there is no setting that could express the opposite", and §213 made that untrue**: Workshop p.261 gives a module builder an Object View Mode and an option to toggle between the two, so a widget in one module *can* withhold the switch. The guarantee survives where it was actually about the reader — the Object Explorer and the traversal dialog take the defaults, and the widget's own toggle defaults to on, so withholding it takes a deliberate act about one widget. What is not affected is anywhere a reader reaches an object through the platform rather than through somebody's module |
| **Full** form factor — comprehensive | ✅ | (p.3) |
| **Panel** form factor — for embedding in other applications, focused on critical data | ◑ | (p.4) — stored and separately addressable (db 0046), so the two never collide. **The full form factor is now embedded** by Workshop's Object View widget (§213); the panel one still is not, and what blocks it is not the widget: Workshop p.263's panel behaviours switch between an object view and an object-*set* view, and this platform has no object-set view to switch to |
| Version management for configured views | ✅ | the module's own — a view is a *pointer* at a module, so publishing, versions, revert and the changelog are all the ones §120 and §71 built |
| Branching object views | ○ | TOC §6 — out of scope, tracks Global Branching |
| Generate Object View URLs | ✅ §309 | TOC §20. `apps/web/src/lib/object-links.ts`, `e2e/test_object_links.py`. The Explorer's *search* has been linkable since saved searches were built — "send me the link to that" is why they exist — and the object opened **from** that search was not: it lived in `useState`, so a reader who found something could send the search and a sentence saying which row. The link carries **both** ids because the instance store is partitioned by object type, so there is no read that takes an instance id alone. The URL is the state and the fetched instance only a cache, which is what keeps the dialog and the address bar from disagreeing. **Written with `replace`, not `push`** — `useUrlState`'s stated choice, so the link is shareable without burying the page somebody arrived from; the consequence, asserted rather than assumed, is that Back leaves the Explorer instead of closing the object. A link to a deleted object and a link cut short by a chat client are told apart, because one is gone and the other can be asked for again. **This is also the prerequisite for favouriting an object** (the row above): a favourite is a shortcut, and a shortcut needs somewhere to point |
| Comment on objects | ◑ §322 | (`object-views` p.137; db 0078) — all three of p.137's capabilities, behind p.137's button: "Object Explorer allows users to comment on an object, mention other users, and attach files and images… using the **View comments** button in the header of any Object View." **"Any" is the whole placement claim**, and this platform has two renderings of an Object View — the generated one and somebody's Workshop module — so the button sits above both, for the reason §312 found putting p.34's star "next to its title": anything inside either rendering exists for one kind of object and not the other. **Mentions are resolved on the server from the text**, against the workspace's own members, and that is a security decision rather than a tidiness one: a client that could send user ids beside the body could have a comment delivered to somebody who cannot see the object it is about. **Longest match wins** — display names contain spaces, so `@Ada` and `@Ada Lovelace` are both readings of the same characters and the longer one is what was typed — and an `@` matching nobody stays literal text, because guessing would interrupt somebody whose name merely starts the same way. The **span** travels with the mention so the thread marks exactly those characters (§146: a browser re-finding the name would be a second matcher, free to disagree with the one that decided who was notified). A mention notifies, **except your own** — naming yourself is a way of writing, not a request to be interrupted — and the notification links to the object through §309's one-key URL. The thread reads **oldest first**, the opposite of every other listing here, because a conversation read backwards is a different conversation. ○ for **editing and deleting a comment**: p.137 describes neither, and a thread's affordances are the kind of thing to copy rather than invent. `apps/api/tests/test_object_comments.py`, `apps/web/src/lib/object-comments.test.ts`, `e2e/test_object_comments.py` |

**Build order.** Standard views first — they are generated, they need no builder UI, and they immediately make every object type navigable. Configured views second, reusing the Workshop runtime bound to a single object. The panel form factor last, since it exists to be embedded and there is nothing to embed it in yet.

**Configured views are built (§144), and the reason they were cheap is that they are a pointer.** `object_type_views` stores which module stands in for which type and which of its variables receives the object; everything a view needs beyond that — layout, variables, events, versions, publishing, changelog — is the module's, already built. Four refusals at save time keep a view from being configured that nobody could open: an unknown form factor, a module this workspace cannot see, an **unpublished** module (an object view is read by whoever can read the object), and a `subject_variable` that is not a `single_object` variable of that module.

---

## 5. Action types (`action-types`)

> "An action is a single transaction that changes the properties of one or more objects, based on a user-defined logic… An action type is the definition of a set of changes or edits to objects, property values, and links that a user can take at once. **It also includes the side effect behaviors that occur with action submission.**" (p.2)

Ours: **parameters and rules** (§127), run from a Workshop `run_action` effect. Migration 0044 split what used to be one list of property names into the inputs and what is done with them.

### 5.1 Core

| Feature | Status | Notes |
|---|---|---|
| Edit property values on one object | ✅ | |
| Edit **multiple objects in one transaction** | ✅ | §134–§141 — an action can change, create and delete several objects of several types, across as many datasets, in one transaction (decision 0008) |
| **Create and delete objects** | ✅ | §135, §138–§141 — create any type with a dataset in this project, delete or change the object the action ran on *or* one a parameter names, all in one transaction (decision 0008) |
| **Create and delete links** | ◑ | §136, §142 — a link here *is* the join property (migration 0027), so a link rule writes or clears it, from either end: the far side names the other object through a parameter and writes *its* join property. Many-to-many is refused with a sentence |
| **Parameters** — typed user inputs with their own form | ✅ | §127, §129, §130 — the model executes, is editable through the API, and the form renders one input per *visible* parameter (p.25). **§330 corrected a tick that was too generous**: an `object` parameter drew a text box, so the form asked whoever submitted the action to know a uuid and type it in — §214's control that looks like it works, sitting under a ✅ since db 0044. db 0083 gives the parameter its own object type and the form a dropdown of that type's objects, and p.34's other sentence comes with it: the submitted value is checked against the type before the action runs, on the path with no screen as well as the one with. **The type is only ever the declared one.** `object_parameter_types` can also infer it from the action's rules — its docstring has said since §257 that "the day `action_parameters` grows an `object_type` column this reads it instead and the guess goes" — and the first draft used that inference as a fallback so older parameters would get a dropdown too. The existing tests refused it in a minute: the guess is wrong for a link rule, which names the far object through the link type rather than through `config.object_type`. A wrong guess renders a gap in a notification and is survivable there; it refuses the wrong submission here. So an untyped parameter keeps exactly the box and the behaviour it had, and declaring the type is what turns on both halves at once. A dropdown that cannot hold everything says so and points at p.36's filter (§256, one control down), and an empty one names the type rather than leaving a reader unable to tell "none" from "failed to load". **The sweep scored 15/0, 20/0 and 11/0**, and the survivor worth naming was the same one twice: every fixture had two objects, so nothing reached the cap and a build that never reported truncation survived both the service and the screen — the note is drawn in one layer and decided in the other, so it had to be closed in both. A mutant in that sweep also has to *compile*: one replaced a call with an undefined name, every test died on a NameError, and it was scored caught for a reason unrelated to the check. `apps/api/tests/test_action_choices.py`, `apps/web/src/lib/action-choices.test.ts`, `e2e/test_action_choices.py` |
| Parameter default values | ✅ | §127, §129 — applied on execute (p.27) and settable through the definition endpoint |
| **Filter the results of a parameter dropdown** | ◑ §331, §332 | (`action-types` p.33-37, p.40-41; db 0084) — p.36's "only shows objects where the specified property matches any of the provided values", with §330's dropdown underneath it. **A filter compiles into an object set, which is what p.41 says Foundry does too** ("the object dropdown validation is converted into an object set") — not borrowed for convenience: this platform already has one way to narrow a set of objects, it runs identically against both stores, and a second narrowing written here would be free to disagree with it. p.36's OR over values *is* the `in` operator; p.36's several filters narrowing together *is* a tuple of `Filter`. **The dropdown and the refusal are the same set evaluated twice**, which is p.34's sentence read whole: §330 checked the parameter's type, and a dropdown narrowed to one object beside a check that accepts any object of that type is §214's control that looks like it works. A jsonb column rather than §329's child table, and the difference is the argument — an override block's order *is* the rule, so those rows have identity and a position, while filters narrow commutatively and a table would give them an identity nothing reads. **An unresolved filter yields nothing rather than everything**, which p.36 does not decide: a filter may read a box nobody has filled in, and showing every object then would offer exactly the ones the filter exists to exclude — so the dropdown is empty, names the box that comes first, and the submit check fails closed for the same reason. **p.40-41 is the security half and it is a real rule here.** Static filter values are readable by anyone who can read the action type — Foundry's example reveals that an investigation exists to people who cannot see a document in it — so a caller who may not edit the action receives none of them, on the *listing* as well as the single read, since the listing is the route a screen loads without being asked. The whole list goes rather than just the values, because a filter reduced to its properties still says what is being filtered by what. They lose nothing: this form asks for the resulting objects and never for the filter, which is precisely the leak p.41 admits Foundry could not close ("users could review the network request containing this object set"). **○:** p.36's third value kind (a property of an object-reference parameter), and p.33's multiple-choice options from an object set, which needs a parameter kind this platform does not have. p.37's Search Arounds and p.36's "changing the starting object set" have their own row below (§333). **The sweep scored 22/2, 17/1 and 11/1**, and writing the browser test found a defect no assertion had reached: the choices query is keyed on the values its filters read, so changing one left the data undefined for a round trip and the control flipped from a dropdown to a *text box* and back on every keystroke in the box above it — worse than a stale list, because a text box accepts an id the filter excludes. The survivors were all instances of one thing, now written into `STATUS.md`: **a reused pattern does not bring its tests.** Twice for a `{kind, …}` guard that is indistinguishable from a non-empty-name check (§328 and §329 each found it before), and once for a per-parameter document the editor seeds — §329 found *that* one, wrote the test, and the test did not travel when the pattern did. **§332 is what that redaction cost, found by reading it back rather than by any suite.** The form works out which boxes to re-ask its dropdown on by walking the filters — which a viewer is not sent — so for exactly the readers p.40-41 exists to protect, p.36's "inferred from another parameter" was dead: choose a region and the Team dropdown stays on "choose Region first" forever. The server now sends `dropdown_watches` beside the filters and `redact` is gone; `for_reader(parameter, may_edit=…)` does both, because a redaction that can be applied without supplying the replacement is one that will be. The names give away a parameter of an action the reader already holds in full — not p.40's "property value combination", and a dependency the form's own behaviour states on the first keystroke either way. And `effective-parameters` was a **third door**: a `viewer` route returning the same parameter shape, unredacted while the other two were closed, which is the leak p.41 says Foundry could not close and the row above claimed this build did not have. **Both were invisible for one reason**: every browser test in the suite ran as the owner, so a screen that is only wrong for a reader had never had a browser in front of it. `viewer_page` is the fixture that ends that, and its first test differs from the owner's only in who is holding the mouse. §332 swept **9 caught, 4 caught, 2 caught, 0 survivors** across the three layers. **○:** the harness has one viewer session and no editor one, so "what a reader is shown" is now reachable and "what an editor is shown that an admin is not" still is not. `apps/api/tests/test_action_filters.py`, `apps/web/src/lib/action-filters.test.ts`, `e2e/test_action_filters.py`. |
| **Search Arounds and the dropdown's starting set** | ◑ §333 | (`action-types` p.34, p.36-37; db 0085) — p.37's "a Search Around would create a new set by traversing a link on every object in the current set", with the row above's filters narrowing whatever it reaches. **db 0084's filters narrow a set; this says which set**, and p.37's own example cannot be written as a filter at all: "the Github Issues of *this* Employee" is not a property comparison, it is a walk, and the thing walked from is a value in the form. **A source compiles into the same nested `ObjectSet` a Workshop variable builds.** `Traversal` has been the shape of a hop since §155 and `resolve_traversal` has evaluated one against both stores since then, so p.37 asks for nothing that is not already there — the unit's real work was moving that resolver out of `routes/objects.py`, where `action_choices` could not reach it, and refusing to write a second one beside the action code. The one rule that had to stop being duplicated is **which end of a link a walk reaches**: `object_sets.far_end` now answers it for `derived_properties` and for this, because a third copy free to disagree with migration 0027's join is one too many. Direction is derived from the type reached so far, which is why no definition in this build names one and none can name the wrong one. **The landing type is checked against §330's column rather than trusted** — a walk that ends anywhere else offers objects the parameter cannot hold, and p.34's own validation would refuse every one of them a moment after somebody picked one. The walk is normalised rather than echoed for the reason `derived_properties.parse` gives: an API that cannot accept its own output back makes read-modify-write impossible. One jsonb column and not a table of hops, and unlike 0084 the argument is not commutativity — hops are emphatically ordered. It is that a chain is read and written whole: nothing on this platform adds a hop to the middle of a walk. NULL rather than `'{}'`, because "every object of the type" is a fact about the parameter and not a hole in it. p.36's parameter start is the half that makes p.37's example writable, and it is redacted with the filters (p.40-41): a walk names object types and link types, and "somebody is offering the Documents linked to this Investigation" is the same combination p.40 is about. What survives is the parameter *name*, which is §332's rule — a form told nothing re-asks nothing. **A page-size defect §331 shipped, found while wiring this.** The submit-time check evaluated the narrowed set at `MAX_CHOICES` and searched the result, so an object matching the filter but sitting past the fiftieth was refused: the answer to "is this value allowed" depended on how many the *control* can hold, which is §256's trap arriving through the back door. It asks about the one object now, by key. **○:** p.36's ObjectReference *list* parameter as a starting set, which needs a list-valued parameter kind this platform does not have; and §325's cleanup queue does not yet report an action parameter as a reason a link type will not go — a jsonb column carries no foreign key, so a deleted link becomes a dropdown that refuses to build rather than a deletion that refuses. **The sweep scored 39/3, 20/1 and BROWSERSCORE**, and every survivor was a check that could not fail: a `{kind, …}` guard indistinguishable from a non-empty-name check for the **seventh** time across §328-§333; a walk whose start reached nothing offering *everything*, which is decision 0002's silent widening; a hop-cap test whose four-hop chain also failed to join up, so deleting the cap still refused it and the assertion ("3" appears in the message) held either way; and a `startableParameters` fixture with no parameter that was an object without a type or a type without being an object. Writing the panel reproduced the pattern `STATUS.md` names for the **sixth** time — the definition dialog saves the parameters whole, so a document it does not read is one it overwrites with nothing, and the first draft showed "Every object of this type" over a saved walk. The browser test written for exactly that reason caught it. `apps/api/tests/test_action_search_arounds.py`, `apps/web/src/lib/action-search-arounds.test.ts`, `e2e/test_action_search_arounds.py`. |
| **Parameter configuration overrides** | ◑ §329 | (`action-types` p.43-46; db 0082) — p.43's "change a parameter's behavior and configuration under specific circumstances… removing the need to configure separate action types with only minor variations", in p.43's own example: a justification that is **required and visible for managers, hidden and optional for the assignee**. **This is not §328 one table along, and the difference is the whole unit.** A section changes what a form *looks like*; an override changes what the action *asks for*, so the resolution runs inside `bind_parameters` before anything decides whether a submission is complete — applying it for drawing only would let the manager submit without the thing p.43 says they owe, and the screen would be the only place the rule existed. A browser test asserts the field appearing *and* the submission being refused without it, and a second one submits through the API with no browser at all, because that is the one way of getting this wrong that looks right from a screenshot. **p.45, twice on two pages: "if more than one is true, only the first one will be executed"** — not a merge, and the rest are not consulted. **NULL means "leave this alone", not false**: p.43's block makes one parameter required *and* visible together, so a block that only hid something would otherwise un-require it as a side effect — which is why the panel's controls are three-state selects rather than checkboxes. p.45's one difference from a submission criterion is a refusal at save time ("only parameters which appear above the current parameter in the form hierarchy can be referenced"), the hierarchy being §328's form; that rule is also **what makes a single resolution pass enough**, because every value a block may read is settled before the block is reached — without it the thing being computed is a fixed point, and a form whose parameters are required in a cycle has no answer at all. A condition that cannot be read leaves the parameter exactly as configured, which is the *opposite* direction from §328's sections and deliberately so: a section shown on a broken rule puts a box in front of somebody, while an override applied on one would demand something of them. p.45's "a warning will be shown on the override itself" is a warning rather than a refusal, as p.45 has it — the block is legal and simply does nothing. **○:** p.45's fourth overridable thing, constraints, which is absent because the thing it would override is — a parameter here has a type, a default, a `required` and a `hidden`, and no value constraints for a block to narrow. `apps/api/tests/test_action_overrides.py`, `apps/web/src/lib/action-overrides.test.ts`, `e2e/test_action_overrides.py`. **The sweep scored 19/0 on the service, 25/0 on the wording and 17/0 on the screen**, and got there through twelve survivors that were mostly one finding wearing different clothes. Three said the same thing: every check in the API file asked what the *form* would be told, so removing the resolution from `bind_parameters` altogether — or passing it an empty user — left the suite green while p.43's rule quietly became decoration. There is now an action over a real object and three submissions made without a screen: one refused for want of an overridden requirement, one whose overridden default reaches the stored row, and one accepted for a different person. Three more were the same gap on the browser side, around the "has somebody typed here" tracking that keeps a re-resolved default from rewriting a sentence under the person writing it — and the record-switching half of it, since that tracking is a fact about a form being filled in rather than about a parameter. Two were **false never appearing in a test**: no block anywhere set a field to `False`, so `is not None` and plain truthiness were indistinguishable in both languages — and `set_required: False` is p.43's assignee, the other half of the sentence the unit is built on. Two were unreachable rather than untested and went with the reasoning in their place (§213): a block with no conditions was refused twice, the request model answering first with a field-path list nobody can act on; and `write_overrides` opened by deleting rows that `ON DELETE CASCADE` had already taken — §213 arriving from the database rather than from another layer of Python. One was a **check that was a coin flip**: with every block written at position zero the query breaks the tie on a random uuid, so reading the list in order passed about half the time. And the last would have met a builder on their second visit — a dialog that did not load the blocks would delete every override the moment somebody opened it to fix a typo elsewhere, which every other test missed because they all write a block into an action that had none. |
| **Rules** — the logic mapping parameters to edits | ✅ | §127, §135–§143 — all five of p.75's simple rules execute against the subject or an object a parameter names, and the editor offers every shape |
| **Submission criteria** — conditions that must hold for submission | ◑ | §128–§131 — conditions over parameters and the current user, checked before the first write (p.49–56), editable in the Ontology Manager; no nesting (p.56's all / any / none) |
| **Validation** — e.g. only HR may perform this action | ◑ | §128 — this *is* submission criteria (p.140: "simple submission criteria can require a specific user ID or group ID"); a criterion can require a group |
| **Configure sections in the action form** | ◑ §328 | (`action-types` p.122-124; db 0081) — p.122's "logical grouping of parameters to organize an action form", with p.123's one-or-two columns, always-shown description, collapsing, and hiding both plain and conditional. **A section changes what a form looks like and nothing else**, and every layer says so in the same words: an action with every section deleted submits exactly the same values from exactly the same parameters, so a hidden section's parameters are still seeded, still sent and still validated. That is not a detail — a form which quietly dropped them would mean the same action did different things depending on which boxes were on screen — and the browser check that proves it is a criterion that refuses when the hidden value never arrives, because a parameter merely written back unchanged produces the same stored row whether the form sent it or not. The table sits **beside `action_parameters` rather than as columns on it**: a parameter belongs to at most one section, and a form with no sections at all is the shape every action here already has. `ON DELETE SET NULL` returns a deleted section's parameters to the form body, since an arrangement that could delete the thing it arranges would make tidying a form a destructive act. **p.123's conditional override is decision 0007's `{left, operator, right}`** — the same shape a submission criterion uses — so the form *asks* the server which sections to draw rather than evaluating it in TypeScript, which is the argument `POST .../check` has made since §130 and the reason there is one condition grammar rather than two. It costs almost nothing: the browser reads the conditions for the parameters they *mention* (a question about the document's shape, not its meaning), keys the query on just those values, and a form whose sections carry no condition never makes the round trip at all. Two cases p.123 leaves open are decided rather than left to fall out — **a condition that cannot be read hides the section**, since `check_criteria` fails an unevaluable criterion closed and a section shown on the strength of a broken rule would put parameters in front of somebody for no reason anyone could state; and **a required parameter inside a section the form is not showing** leaves the button disabled, which is true, and says so in a sentence addressed to whoever arranged the form, because "New status is required" beside no New status box is §214's control that looks like it works. The Form tab writes the whole document (p.124 describes dragging parameters between sections, and granular edits pass through forms that do not make sense), after the definition and only if it landed — a section names its parameters by api_name, so one holding a parameter the same save is adding can only be written once the parameter exists; a rename and a removal both carry through the sections for the reason `patchParameter` already carries them through rules and criteria. **○:** p.124's single Form Content order across sections *and* loose parameters — this build stores two orderings, so the body is drawn first and the sections after; interleaving them is a change to the document rather than to the layout function. `apps/api/tests/test_action_sections.py`, `apps/web/src/lib/action-sections.test.ts`, `e2e/test_action_sections.py`. **The sweep scored 17/2 on the service, 45/0 on the arrangement and 24/2 on the screen**, and the browser survivors were the two that mattered. The first was a real defect and nothing in three layers of tests had an opinion about it: p.50 gives two condition templates and only one names a parameter, so a section shown to the person it names (p.140's user or group id, the same grammar p.123 reuses) mentioned nothing the form could watch — the query stayed disabled and that section could never be drawn, for anyone. *Whether there is a question* and *when the answer goes stale* had been folded into one function; they are two now, and the browser check was verified by reintroducing both lines and watching it go red rather than assumed to catch anything. The second pair were checks that asserted a label rather than the thing the label is about: a two-column section was read off `data-columns="2"`, an attribute the component writes about itself, so a mutant that kept it and dropped the grid survived — it measures the fields now, and asserts the one-column layout stacks them, because "side by side" alone would pass for a form that ignored the setting. And "a plain form never asks the server" is a claim about the network that no assertion about the screen can reach, which is §327's finding one unit later; the test watches the request and opens a conditional module afterwards, because "nothing was asked" passes just as well for a listener attached to the wrong thing. The service's two were unreachable rather than untested — p.123's column count refused three times over, and a "checked before anything is written" that the transaction already guarantees — and both were deleted with the reasoning in their place (§213), the second after a mutant moved the `DELETE` to the top of the function and changed nothing anybody could observe. **And the worst defect of the unit was found by reading rather than by any of that**: `set_definition` replaces every `action_parameters` row, so `PUT .../definition` left every section standing and its contents gone. No test saw it because the only thing that saves a definition is the editor, and the editor writes the form immediately afterwards and put it all back — the two-request ordering the dialog needs for a different reason was hiding a bug underneath it, and the API had carried it since the day the table landed. Membership is now carried across the rewrite by api_name, and the half that is *not* carried — a parameter renamed by the same save — is asserted too, so changing it would be a decision rather than a discovery. |
| Actions on interfaces / on structs | ○ | TOC §13–14 |

### 5.2 Side effects (TOC §18–23) — all ○

| Feature | Notes |
|---|---|
| **Notifications** | ◑ §257 — (`action-types` p.87-101) **A rule, not a resource**: p.89 puts notifications in the same *Add new rule* dropdown as the other five, so `notify` is a sixth `action_rule_kind` (db 0066) and the delivered notification is a row per recipient (p.90's "sent to each recipient individually"). **Built:** p.90's three structured recipient kinds — a static list, a user-id parameter, and a property of an object parameter (p.100's own tutorial case); p.91's subject, body and optional link with p.92's triple-handlebar references to parameters, an object parameter's property, and p.101's `Recipient` and `Current User`; p.95's truncation-with-ellipsis rather than refusal, because the length depends on the data; and p.96's two permission modes, with the default one refusing the **whole action** before anything is written, since "no data will be edited" is not expressible afterwards. The content is rendered against the ontology **before** the action's edits (p.92), which is why it is rendered in the pre-write block. **⊘ rather than ○:** `From a function` recipients and content (p.90, p.92) need Functions, which §1.3 marks ○ — an option whose only outcome is a save that fails is worse than an absent one; and **email delivery**, with p.92's Advanced Email Configuration and p.95's redaction, because there is no mail gateway here. p.91 makes in-platform delivery the other half of the same feature, so that half is built whole rather than both halves partly. **The inbox is built** (§257): a bell in the platform bar with p.91's badge and "See all", where a notification's link is only drawn as a button when it is a path inside this app — p.92's handlebars make the URL a template, and part of a template comes from a property value. **The editor is built** (§258): `notify` is the sixth kind in p.89's *Add new rule* dropdown, with p.95's recipient selector, p.91's three content fields and p.96's permission mode — and p.94's reference buttons over each template, because somebody typing `{{{}}}` by hand types two braces and two braces are not a reference. The people offered are everybody `effective_workspace_role` grants access to (`GET /workspaces/{id}/notification-recipients`), which is the same predicate p.96's check applies at send time: the membership table is a *narrower* rule, and a workspace's creator is in it never, so a picker fed by it offered nobody in exactly the workspace where a rule gets written. **Groups are absent from the selector** though p.95 names them, for `From a function`'s reason one step further on: a group id sent as a recipient reaches a lookup in `users` that finds nobody, so the rule would save and then notify no-one |
| **Webhooks** | ◑ §259, §260, §261, §262 — (decision 0012; `data-connection` p.216-242, `action-types` p.105-111) load-bearing twice, and one implementation serves both: `data-connection.md` §26-29 makes a webhook an outbound call from a *source*, and §5.2 makes it an action side effect. **Built:** the resource — a request shape (method, relative path, query, headers, JSON body) held against a REST connection, which is where p.220 puts the split, so the connection owns the base URL, the auth and the secret and the webhook owns the rest; p.228's input parameters and p.229's outputs, extracted by the dotted path the REST connector already understands; p.222's test call; and p.242's history, scoped by RLS to the person who made the call and with the `store_responses` switch that keeps the status and drops the bodies. p.237's `retryable-status-codes` and its three-valued "may the far end have changed" are stored on every run. **The action rule is built** (§260): p.106's two modes, chosen per rule — *writeback* runs before the write, refuses the whole action when it fails, and at most one may be configured; *side effect* runs after, cannot fail the action, and many may be. p.114's default is the side effect, because a rule added without reading the mode selector must not be able to take an action down. p.107's input mapping from action parameters and static values, and p.110-111's outputs — a writeback's, usable by any *subsequent* rule and by a notification, through the reserved name `webhook.<output>` in the one value namespace every rule already reads from. **A known limitation:** an action whose only rule sends data out is refused as empty, the same answer §257 settled for a notification-only action — what stands in the way is the write path appending a dataset version per execution, which is decision 0008's shape rather than a rule kind's. **The webhook's own screen is built** (§261): p.220's Webhooks section on the source it belongs to, with p.233's request builder, p.228's inputs, p.229's outputs and p.222's test call — which shows the response in full, because p.222's next sentence is that the response is what output parameters get configured from. **The rule's editor is built** (§262): `webhook` is the seventh kind in p.113's *Add new rule* menu, with p.114's mode selector — named for what each mode does rather than for Foundry's word, since p.106's two rows differ in every column — p.115's input mapping from action parameters or static values, and p.111's `webhook.<output>` offered in every value picker *below* the writeback that produces it. p.106's single-writeback rule and p.110's ordering rule are said beside Save rather than on a rule, because the rule that breaks either is not wrong on its own. **⊘:** function-mapped inputs (p.107-110) need Functions; chained multi-call webhooks (p.234-237) are their own unit; the OAuth authorization-code grant (p.243-247) is beyond `RestConfig`'s client-credentials; and p.242's `webhooks:read-privileged-data` has no custom-role mechanism here to hang off, so it is absent rather than approximated |
| Trigger a schedule build | |

### 5.3 Operational

| Feature | Status | Notes |
|---|---|---|
| **Inline edits** | ◑ §238 | (`action-types` p.135–138) — which action types may back a cell-level edit, and submitting a batch of them. p.136–137's requirements read together say one thing: **an eligible action's whole effect is "set these columns on this row"** — one `modify_object` on the subject, every value from a parameter, every parameter a single primitive and none hidden. That is not a checklist bolted onto a feature, it is what makes the feature expressible: a batch of arbitrary actions could not share a dataset version, and a batch that wrote as it went could not be p.138's all-or-nothing. The refusals are **computed on the server and sent on every action type**, so the panel that offers them cannot disagree with the executor that runs them. Two of the criteria are deliberately **absent rather than implemented**: a `modify_object` here reads a parameter and nothing else, so there is no static-value shape to catch, and this platform has no side-effect webhooks to disable — a check that cannot fail is not a check. p.135's "every parameter is optional and defaults to the existing value of the object" is what makes a single-cell edit legal, and is seeded **from the rule** rather than by name match, because p.241 only *recommends* matching names. db 0063 adds `action_runs.batch_id`, which exists because p.242 and decision 0008 pull opposite ways and both hold: **one run per object**, since a run is how this platform answers "who changed this row", and **one dataset version per submission**, since several versions carrying one submission is a history that has to be interpreted. §239 built the surface — p.240–243's Enable inline editing toggle, the parameter-to-column mapping with p.241's automatic match, the Edit table button, staged edits with per-row Undo, and the confirmation dialog with p.243's one-click override — reusing `PropertyInput` as the cell editor, because §237 had just spent a unit deleting the second renderer of typed inputs. **Still ◑, for two things named rather than assumed**: p.241's in-cell dropdown over a parameter's enumerated values, which needs a property's value-type constraint to reach the table before the control could offer it, and p.241's variables passed as action parameters "without the user needing to edit the field in the table" — a variable picker per parameter, which is a different idea from a column mapping |
| **Undo / revert an action** | ◑ §319 | (`action-types` p.154-156; db 0076) — p.154's Undo, in p.154's place: "You can revert an action by selecting Undo in the success message after any successful action application." The form closes on success, so the toast is owned by the page and outlives it, which is the one claim only a browser can check. **A write, not a rewind**: here the dataset is the record and the instance store is a projection of it (decision 0008), so restoring only the projection would look perfect and come silently undone at the next sync — the undo appends a row and updates the index exactly as the apply did, and has a run of its own with an author and a time. One test applies, undoes, **re-syncs**, and asks the object again, which is the only sequence that tells the two implementations apart. **Six refusals, and they are most of what p.154-156 say**: only the person who applied it (p.154); only if it is still the most recent edit to the object, *even if the later edit was to a different property* (p.156) — which is why a run stores the object's whole property map rather than a diff of the keys it wrote; only once (p.155's "your only opportunity"); not when the toggle has been off since, **even if it has been turned back on** (p.155) — a check on `allow_revert` alone would hand every undo back the moment somebody flipped the switch, so switching it off stamps the outstanding runs and the stamp is never cleared; not an undo of an undo, which is the action applied again and would be a button whose name lies; and not a run that created or deleted objects or touched its neighbours, refused **with the sentence saying which** — an undo that half-works is worse than one that says it cannot (§214). ○ for exactly that last case: p.156 treats reverting a delete as supported, and creations, deletions and multi-object runs are named on the run rather than attempted. **The toast does not fade**, and that is a deliberate divergence: p.156 needs a section called "Undoing a delete action without the revert action toast" precisely because Foundry's does, and its two remediations — migrate to a new object type, drop all edits — do not exist here, so a timer would leave somebody with no route at all. `apps/api/tests/test_action_revert.py` (the refusals, no database), `apps/api/tests/test_action_undo.py` (the writing, against a real dataset), `apps/web/src/lib/undo-toast.test.ts`, `e2e/test_action_undo.py` |
| **Action log** | ◑ | we audit; Foundry surfaces it as an ontology feature and a Workshop widget (Action Log Timeline) |
| **Action metrics** | ◑ §323 | (`action-types` p.164-166; db 0079) — p.164's "near real-time usage of an action type over the last 30 days": success and failure counts, the P95 duration, p.165-166's failure categories, and the seven-day run history. **Two windows, and they are p.164's two** — the counts get thirty days and the history seven; each is sent with its answer rather than written into the screen, so a label cannot drift from what was counted. **The unit exists because of one sentence.** p.165: "unlike action logs, action metrics track failures" — and `action_runs` had recorded failures since db 0013 while being unable to show the two that matter. Every refusal here is raised *before* `open_run`, so an invalid parameter and a failed submission criterion, the two things a person actually does wrong, produced a 422 and no trace at all; the only failure the metric could see was a dataset engine error, which is to say a failure nobody submitted. So a refused submission is now recorded as a run that opened and closed in the same breath — **on its own connection, which is the whole mechanism**: the refusal is raised, and a raise unwinds `user_connection`'s transaction, so a row written on the request's connection is rolled back by the very exception it exists to describe, and every count is right at the moment it is taken and wrong a millisecond later. `test_a_refused_submission_survives_the_rollback_it_caused` reads it back on a second connection for exactly that reason. Categories are **stored at write time, never matched out of `error` text later** — a category derived from a message is one that changes when somebody rewords the message, silently and in the past tense. The P95 is `percentile_disc`, so it reports a duration some run actually took, and is `None` rather than nought when nothing has finished: a P95 of 0 says every run was instant. Running runs are counted and reported as themselves, and excluded from the success rate's denominator — folding them into either side would make a slow action appear to get worse for being slow. **○ for p.166's two function-backed categories**, `function` and `user_facing_function`, absent from db 0079's CHECK rather than offered: Functions are ○ here, and a value nothing can produce is a category that sits in every list and never appears (§214). An API drift test ties db 0079's categories to the browser's words for them, since nothing else connects a Python tuple to a TypeScript object. **The sweep scored 44/15, and the fifteen were one defect in fifteen places**: a real run here always finishes, always finishes now, and always takes about the same fraction of a second, so a suite built from real applications cannot tell a thirty-day window from a thirty-year one, a discrete percentile from an interpolated one, a 95th from a median, a `running` run from a failed one, or a two-hundred-row limit from no limit. Runs are now seeded into `action_runs` with chosen ages, durations and statuses — twenty runs of one to twenty seconds give a median of ten and a P95 of nineteen, which is a distribution a percentile can be wrong about. **Two of the survivors were right to survive**, and both were deleted rather than tested: `needsAttention`'s zero-failure guard (with no failures the rate is 100% or `null`, so both remaining clauses are already false) and the P95's `FILTER (WHERE finished_at IS NOT NULL)` (an ordered-set aggregate discards NULL inputs by definition — checked against Postgres rather than assumed). Both read as defensive and neither could fire. `apps/api/tests/test_action_metrics.py`, `apps/web/src/lib/action-metrics.test.ts`, `e2e/test_action_metrics.py` |
| Permissions, read and write authorizations | ✅ | RLS |
| **Upload attachments through an action** | ◑ §237 | (p.127–128) — an `attachment` parameter draws a file picker, the file is uploaded when it is added ("immediately uploaded… once they are added to an action form", p.128), and the action writes the reference the upload returned. **Every piece existed before §237 and the form still rendered a text box**: `pure.inputTypeFor` answers `"text"` for anything it does not name, and `property_values._coerce_attachment` requires the four-field object — so the control could never produce a valid value, which is §214's refusal applied to an input. The fix was deleting the *second* renderer: `PropertyInput` has said "the input for one editable property in an action form" since it was written, and the Canvas form had grown its own field. ○ for p.127's **Allow multiple values**, which needs a list-valued parameter this platform has no concept of — the parameter's type is one of `ontology.PROPERTY_TYPES`, with no arity beside it |
| Upload **media** through an action | ⊘ | (p.125–126) — media *sets* and `media reference` properties, the property type this platform does not have and the one §224 and §235 marked the media widgets ⊘ for. p.125 recommends it over attachments precisely for the things a media set gives (billions of files, transformations, format conversion), all of which are the service rather than the action. Split from the row above in §237, because that row said "media / attachments" and the two halves are in different states — §224's rule about keeping "blocked" and "declined" apart, applied to one line holding both |
| **Function-backed actions** `[fn]` | ○ | TOC §15–17, including batched execution |
| Writeback dataset — "the most up-to-date version of object data with user edits incorporated" | ○ | (p.3) |

The parameter-and-rules model is the big one. Ours conflates "what the user types" with "what gets written"; Foundry separates them, and everything else in this section — validation, submission criteria, defaults, dropdown filtering — hangs off that separation. **Do parameters and rules before any of the features that depend on them.**

**Decided in `docs/decisions/0007-action-parameters-and-rules.md` (§124); the model is built (§127).** Migration 0044 replaced the JSON column with `action_parameters` and `action_rules`, converting every existing action type into parameters named after the properties it wrote plus one `modify_object` rule each — which is what keeps every saved Workshop `run_action` working unchanged, and is tested by running the migration for real against a database seeded at 0043.

Executing an action is now two steps: bind the parameters (defaults, required, unknown names refused), then apply the rules. `hidden` and `default_value` are honoured; the other four rule kinds are storable and refused loudly rather than skipped.

**Submission criteria are built (§128).** Migration 0045 stores one condition per row with its own failure message (p.56); every row must pass (p.50), and the check runs before the run is even opened, so a refused action leaves no dataset version and no history. The operator names are Foundry's own (p.54–55), including "no value" for emptiness. A condition the executor cannot decide **fails** — p.52's warning about NOT conditions is that a check which passes for want of an attribute grants more access than intended.

**The definition is editable through the API (§129)** — `PUT /action-types/{id}/definition` replaces parameters, rules and criteria as one document, validated as a unit, and **refuses removing or renaming a parameter a Workshop module calls**, naming the module. That was decision 0007's last named acceptance test, and it is the reason a whole-document endpoint rather than per-row ones: the three lists constrain each other.

**The action form renders from parameters (§130)**: visible ones get a field, hidden ones are sent but not drawn (p.25), defaults and current values seed them (p.27), required blocks submission, and a refused submission shows the criterion's own failure message (p.56). It deliberately does *not* evaluate criteria itself to grey the button out in advance — that would be a second implementation of a rule governing writes, in another language, free to disagree with the first.

**The editor is in the Ontology Manager (§131, §137, §143)** — one dialog per action, saved as one document, with the server's refusals shown rather than re-implemented in the browser. It offers every rule kind that executes, and every shape each one can take.

**All five of p.75's simple rules execute (§135–§138)**, on decision 0008's boundary: stage every write, commit in one Postgres transaction, one dataset version per dataset per action, and the search index as a projection that is repaired rather than transacted.

**The lookup is built (§139).** A `create_object` rule can name any object type with a dataset mapped in this project; the row goes into *that* dataset, and both datasets commit together or not at all — which is the case decision 0008's `commit_versions` was written for and nothing had exercised until now.

**Naming a second object is built (§140, §141)** — a parameter of type `object` holds an instance, and a `delete_object` or `modify_object` rule can name one. A named modify is checked against *the type it changes* and written into *the source that instance came from*, because two instances of one type can come from two sources with different column mappings; changing an object and deleting the same one is refused at save time, and changing one and deleting another is not.

**Both ends of a link can be named (§142).** A link rule written from the side that holds no foreign key names the other object through a parameter and writes *its* join property with this object's `to_property` — as this action leaves it, so an action that changes the joined-on property and links on it in the same submit does not create a link that stops holding the moment it finishes. `delete_link` clears the same column.

**The dialog can say all of it (§143).** A rule picks which object it writes — this one, or one an `object` parameter names — and its property dropdown comes from *that* type; a create picks the type it creates; a link rule asks "which object to point at" or "which object to link" depending on which end this action's type is. Only `object`-typed parameters are offered where an instance is wanted, because a string one would carry a primary key and the executor looks instances up by id.

What is left of §5: many-to-many links stay refused — one foreign key cannot express one, and this platform has no join table to put the second half in. Nesting criteria (p.56's all / any / none) is a `config` shape and waits for something that asks for it.

---

## 6. Ontology Manager (`ontology-manager`)

| Feature | Status | Notes |
|---|---|---|
| Object type editor | ✅ | §57 |
| **Header search bar, `Cmd+K`** — across object types, properties, link types, action types, shared properties, interfaces, functions | ◑ | §146 — one query across the four kinds that exist here, with `Cmd+K` on the window. §167 added **shared properties**, the fifth of p.28's seven — the one that most needed it, since a shared property is what somebody looks for by name *before* creating a second one that means the same. It is the only kind with no object type to name (p.178), so its hit carries a **usage count** in place of an owner and opens the shared property itself. §172 added **object type groups**, which p.28's list predates — p.262 says separately that "Groups are searchable in Ontology Manager's Search bar and Search bar dialog" — and they take the same shape for the same reason, a member count where the owner would be — except that a group's zero is drawn rather than hidden (p.263), since an empty group is a real answer. §173 gave the hit somewhere to go: a group is the second ownerless kind, and the two cannot share a destination, because a group id opened as a shared property finds nothing and does nothing at all. §252 added **interfaces**, the sixth, with an *implementation* count where the owner would be — the number that decides whether editing this shape is cheap. **And for sixty-four units after that, clicking one opened nothing** (§316): an interface is the third kind with no object type to borrow, the browser's hit union declared six kinds of the seven the server returns, and the click fell past the group branch into the shared property handler — an editor over an id from another table, no error, no empty state. Neither layer could complain, and the reason is worth keeping: the response model types `kind` as `str`, and in TypeScript a `Record<Kind, …>` over a union with a member *missing* is a **complete** record, so `tsc` was satisfied by a label map with a hole in it and the chip rendered blank. §316 gave interfaces a destination, replaced the chain of ternaries with a decision that has a case per kind and no fall-through, and put the two lists of kinds under a drift check — `test_response_type_drift`'s argument about fields, applied to a union. Functions are ○ in §1.3, so there is nothing of theirs to search |
| Search results highlight **which field matched**; arrow-key navigation with previews | ◑ | §146 — the matcher reports the field and the mark lands inside its value, because the browser re-deriving it would be a second matcher free to disagree. Arrow-key navigation and previews are ○ |
| Home page sections: Object types, Link types, Action Types, Shared Properties, Interfaces, Functions | ◑ | (p.29) — five of the six are sections of the ontology page: object types, link types and action types since §35, shared properties §165, value types §168 (p.29 does not list those, and they belong beside shared properties for the reason that row gives), object type groups §172, and **interfaces §253**. Functions are ○ in §1.3, so there is no section to draw. Sections rather than p.29's separate home page, because there is one ontology per workspace here and the page it would divide is the one that already exists |
| Filter by **visibility, development status, indexing issues** | ✅ §315 | (`ontology-manager` p.29) — visibility and development status are built, beside p.262's group filter that was already there; one value per filter, and-ed, which is the group control's shape rather than a new one (p.29 does not say which it is, so the precedent decides). **Visibility is not the status filter under another name**, and the case that proves it is worth keeping: p.255 makes `promoted` set a type's visibility to `prominent` and `visibility_for` raises without ever lowering, so a type promoted and then demoted is prominent and *not* promoted — the one type these two controls disagree about, and if they never disagreed one of them would be worth deleting. It is also why `hidden` matches nothing anywhere: a type's visibility is only ever a consequence of promotion, never a setting. §315 added the third, **indexing issues** — and the sentence that stood here, "it wants indexing state the sync path does not record", was wrong on the day it was written: `object_type_sources.sync_status` has carried exactly that since db 0003, which is what §313 found one unit later while building the column this filter asks about. **Three values, not a checkbox**: `failing` and `unsourced` are p.29's two conditions and they are different problems with different remedies, and `any` is the question somebody opening the control is actually asking — "what needs me". An unrecognised value is **refused**, not ignored: a filter that silently returns everything when it does not understand you is §214's control that looks like it works. It and-s with the other two through the same one-value-per-filter shape, so "prominent types with a broken source" is one request. `apps/api/tests/test_object_type_issues.py`, `e2e/test_object_type_issues.py` |
| **Red error messages in an issue column** for object types that failed to index | ✅ §313 | (p.29) — exactly the kind of thing that saves a support ticket, and it did not need a migration: the state has been in `object_type_sources` since db 0003, unread. **Two numbers rather than one flag**, because p.29 names two conditions — "unregistered *or* have failed to reindex" — and they are different problems with different remedies. **Only the failure is red.** A type nobody has pointed at data is the ordinary state of one somebody is still building, so marking it as an error would make the column red on every new type and teach people to ignore it, which is worse than not having the column. The cell counts and the hover carries the sync's **own words**: "this type has an issue" is a fact nobody can act on. `apps/web/src/lib/object-type-issues.test.ts`, `e2e/test_object_type_issues.py` |
| Back home, with hover quick-links to recently edited types and related resources | ◑ §317 | (p.30) — **the recently-edited half is built; the related-resources half is ○ and named rather than quietly folded in.** p.30's three kinds — object types, link types and action types — most recently edited first, in the search box: there is no Back home button here to hover, because this ontology is one page with sections rather than Foundry's separate home (the p.29 row above says why), and the box somebody opens when they want to get somewhere is the thing that already has the job. **Focus, not hover**, since a hover is a gesture a keyboard and a touchscreen do not have and `Cmd+K` already lands here from anywhere on the page. It does not contradict the empty-search rule beside it: an empty query still searches for nothing — eight things somebody edited is not the ontology. Two of the three kinds could answer this and **one could not**: `link_types` was written in db 0003 without `updated_at`, then gained six editable columns over fifty migrations (`from_property`/`to_property` 0027, the side names 0043, `status`/`deprecation` 0055) — every one a way to change a link type with no record that anything had changed. db 0075 adds the column, **backfilled from `created_at` rather than `now()`**, because a default would have told every link type in every workspace it was edited the moment the migration ran. Each row says *when*, because an order says which is most recent and nothing about whether the top row is four minutes old or four months, and those are different lists. `apps/api/tests/test_ontology_recent.py`, `apps/web/src/lib/recently-edited.test.ts`, `e2e/test_ontology_recent.py` |
| **Change management** — save changes, review and restore | ◑ | §85 gives us history; Foundry has an explicit review-and-restore flow (TOC §7–8). **A restore now restores the whole definition** (§166): the snapshot recorded six fields per property, so rolling back silently dropped `visibility`, `value_format`, `conditional_format`, `edit_only`, `derivation` and `shared_property_id` — five shipped features losing their configuration with no error, for five units, because a missing key has no general test. It has one test per field now. Two references can vanish between a version and its restore and are treated differently on purpose: a deleted **shared property** is dropped (p.185 already says its users revert to regular properties), a **derivation** whose links have gone is refused. Versions written before §166 still hold six keys and still lose the rest — that data was never captured |
| **Export, edit, and import an ontology** | ◑ §326 | (`ontology-manager` p.65-67) — p.65's JSON file, exported and imported back. **Written by api_name, never by id**, and that is the whole shape of the document: p.65's second workflow is "copy the working state of one Ontology to another", so every identifier has to mean something in a workspace where none of this copy's ids exist. A test asserts it over the whole file — the ontology half contains no uuid at all. The title property travels as its property's api_name, a link type names both ends by name and carries its join, and an action type brings its parameters, rules and criteria. `created_by`, `created_at` and `resource_id` are absent: facts about this copy, not about the ontology. **p.66's import has nowhere to land here**, because Foundry stages the file into a working state and counts unsaved changes, and an ontology edit here is written when it is made — so the import is split rather than pretended: `plan` says what the file would do and writes nothing, `apply` does it, and p.66's "number of changes that need to be saved" is the plan's count. The plan is computed against an *export of the workspace as it is*, so both sides are the same shape by construction and a straight re-import of an untouched export plans zero changes — which is the one assertion that says the exporter and the importer agree about what an ontology is. **"Recreate the entire working state" stops short of deleting**: a type absent from the file is named and left alone, because deleting an object type deletes its objects immediately and with no review, which is what Foundry's staging exists to prevent — §325's cleanup queue is where that decision belongs. **p.67's `UnreferencedRuleSets` refusal has no cause here** and is deliberately not implemented: Foundry's conditional formatting references rule sets outside the ontology, while this platform's is inline jsonb naming sibling properties, so the refusal would be a check that can never be right (§214). The same class of failure is real and is refused — a rule, link or action naming something the file does not define. **The screen is built** (§327): p.66's Advanced settings page, with Export downloading the file and Import reading one from the drive, showing p.66's count, and applying only when somebody presses Apply — the plan is this platform's staging, since there is no working state to stage into. A file that is not JSON is refused by the page rather than posted, and a file whose contents the server refuses shows the server's own sentence, since p.65's whole premise is somebody editing JSON in a text editor. The screen also carries the warning the count cannot: a file that leaves an object type out says it is **left alone** and points at §325's cleanup queue, because the page somebody arrived from says import "will recreate the entire working state" and the one thing they must not believe is that those types are gone. **○:** applying link and action types, which each need their own resolution pass (a link joins two types that may both be new in the same file); they are reported as not applied rather than skipped silently. `apps/api/tests/test_ontology_export.py`, `apps/api/tests/test_ontology_import.py`, `apps/web/src/lib/ontology-transfer.test.ts`, `e2e/test_ontology_transfer.py`. **The sweep scored 25/4**, and the worst survivor was the test this unit was built around: "re-importing an untouched export plans no changes" checked nothing, because `Fixture()` makes a *fresh* workspace and the export was an empty document — so "no changes" was true for want of anything to change, and a planner calling every common name "changed" survived for want of any common names. It now builds a type, its properties, its formatting and an action first, asserts the export is non-empty, and checks the round-tripped names land in `unchanged`, which is what separates "nothing differs" from "nothing was compared". Two more were the familiar shape — nothing exercised the *action* arm of the reference check (links and titles had tests; actions are a third loop over a third list), and the property-order test agreed with the alphabet, so dropping `sort_order` changed nothing. The fourth was a branch the driver hides: `psycopg` hands `jsonb` back as objects, so `_json`'s string arm never runs against this database — not dead, since the same column arrives as text under another driver setting, so it is checked directly rather than through a query . **The screen's own sweep scored 21/1**, and that survivor took three attempts because the first two checks could not fail: a mutant that shows "that file isn't JSON" *and* posts the file anyway leaves both the page's message and the empty plan intact, since the server refuses an unreadable file too — and asserting the *server's* refusal is absent cannot fail either, because it arrives a round trip later than the page's own message. That is §318's trap, walked into while citing §318 in the comment above it. "Was not sent" is a claim about the network, so the test watches the request, and posts a readable file afterwards to prove the listener fires |
| **Ontology cleanup** | ◑ §325 | (`ontology-manager` p.68-74; db 0080) — p.68's "safe way to delete object types": the flags that make a type a candidate, p.70's ordering by "the highest priority among the flags that an object type triggers", and p.71's Snooze, Deprecate and Delete. **Nothing is stored except the snooze**, and that is the design rather than a shortcut: every flag p.73-74 names is computed from what the platform already records — the deprecation deadline on `object_types`, the sync state on `object_type_sources` (§315's two issues), the description column, the display name, and §320's thirty-day usage. A materialised queue would be a second copy of all of it, wrong the moment anything it summarised changed. The snooze is stored because p.71 makes it a fact about a *person* — "an action that will affect only the user that performs it" — so db 0080 keys on both and the row policy enforces it rather than a handler, which is the one claim two people are needed to check. **Two flags diverge and say so**: p.74's "Trashed datasource" becomes `no_source`, since there is no trash here and a source is mapped or it is not; and "Phonograph deindexed" is Object Storage v1 only, with p.74 stating there is no v2 equivalent, so §315's failing-source check goes in under its own name rather than Foundry's. **One flag p.74 does not list**, which p.73 licenses ("not exhaustive"): `unused`. §320 counts interactions per type over thirty days, and nobody-has-touched-this is a stronger safety signal than anything on the page, because it answers p.68's actual question — would deleting this be noticed. p.74's regex flag is narrowed to its own two default markers rather than accepting a pattern: running a regex somebody typed against every object type is a query whose cost the writer cannot see. The queue is a page (§256), capped **after** ranking — a `LIMIT` in the statement would return an arbitrary two hundred rather than p.70's worst two hundred, a sample of a queue rather than a page of one. `editor` rather than `viewer`, the only read in `objects.py` with that floor: every other listing says what the ontology *is*, and p.71's three buttons are all writes. **○ for p.72-73's configurable flag set and priorities** — a per-user preference over which flags are on and in what order, and the queue is worth having before the panel that tunes it. `apps/api/tests/test_ontology_cleanup.py`, `apps/web/src/lib/ontology-cleanup.test.ts`, `e2e/test_ontology_cleanup.py` . **The sweep scored 43/6**, and the most useful survivor was an absence: every test asked whether a *flagged* type appears and none asked whether an unflagged one stays out, so a queue listing the entire ontology would have passed all of them — on a screen whose buttons delete things. Two more were unreachable branches (no two candidates ever tied, so the secondary sort by name had nothing to order; nothing came near two hundred candidates, so the page cap never bit). **One was dead code and was deleted**: `alsoText`'s plural branch, since "and ${1} more" is already "and 1 more" — while `snoozeText`'s plural stayed, because "Back tomorrow" really is a different sentence, and the sweep catching that one is what says the two cases differ. **One survivor was kept deliberately**: the snooze join's `s.user_id = :uid` is belt to db 0080's braces and cannot be made to matter while the row policy gets there first — unlike §323's redundant P95 filter, which was a no-op by the aggregate's own semantics. **And the last one exposed a worse problem than itself**: fixing a race in the delete-confirmation test meant asking the server instead of the table, which showed that *both* delete tests read `/object-types` and looked for the row — but that listing is a page (§256), this workspace holds thousands of types, and a type made moments ago is almost never on it. "Not in the list" was true before the delete as well as after |
| **Usage metrics** — reads, writes, interactions over 30 days, per object and link type | ◑ §320 | (p.32-34; db 0077) — p.32's four numbers for an **object type**, on the type's own page. p.33 says what they are for and it decides the whole shape: "enabling Ontology users to quickly understand the implications of making a breaking change to this resource". Not analytics — a figure somebody reads *before* renaming a property, which is why the panel leads with **active users** rather than with p.32's defining order: thirty reads by one person and thirty by thirty people are the same `reads` and a different answer. **A counter, not a log**, because p.32's own rule makes the *request* the unit — "many objects loaded or aggregated at once will only be recorded as a single read" — so a row per request would log exactly what p.32 says not to count, and an Explorer left open would write one per keystroke. The grain is (type, person, day, application), the narrowest from which all four fall out; `interactions` is computed rather than stored, since a third number is free to disagree with the two it came from. **p.32's exclusion is honoured where it is decidable**: "any object type or link type usage happening in Ontology Manager is not included", and the Manager's type page and the Object Explorer list a type's objects through the *same route* — so the application is a label the caller passes, and the browser test is the only place that can check the type page passes it. Getting that wrong would make the person deciding on a rename the type's most active user for having looked. ○ for **link types** (the same table would serve, but no read path names one), for p.33's separate **Usage tab** behind a "See more" link (there is one page per type here rather than a tabbed resource view, so the split would be a navigation step invented to match a layout), and for p.34's **Ontology metrics toggle** in Control Panel, which exists because Foundry's figures come from internal tables that need configuring — ours are always on, so a switch would be a control with nothing behind it (§214). `apps/api/tests/test_object_type_usage.py`, `apps/web/src/lib/usage-metrics.test.ts`, `e2e/test_usage_metrics.py` |

---

## 7. Build order

1. ~~**Property visibility**~~ — **done (`STATUS.md` §121)**. Stored, editable, and honoured by the Object Explorer, which no longer draws a hidden property's column. Deliberately **a display hint and not a permission**: the value is still stored, still synced and still returned by the API, exactly as Foundry's "an indication to user applications" (p.111) describes. Making it look like access control would be worse than not having it, because somebody would use it as one.
2. ~~**Standard Object Views.**~~ — **done (`STATUS.md` §122, §147, §149)**. Generated from the object type, no builder UI, no saved document. §4.1's table is the detail: prominent properties above a table of the rest, a geopoint on a map, a time series as a chart. Was still written here as "now unblocked" long after it shipped — see the note under this list.
3. ~~**Link type per-side display names and self-links**~~ — **done (`STATUS.md` §123)**, less the one test named above. Self-links turned out to already work; only the naming was missing.
4. ~~**Action parameters and rules.**~~ — **done (`STATUS.md` §127–§143)**, and with it the transaction boundary it turned out to need. Decisions 0007 and 0008 both read "decided and **built**": migration 0044 split the one JSON column into `action_parameters` and `action_rules`, 0045 added submission criteria, and all five of p.75's simple rules execute in one Postgres transaction with one dataset version per dataset per action. **This line said "designed and next to build" for sixteen units**, and a recommendation was made from it before anybody opened the decision record it cites — see the note under this list. What is genuinely left in §5 is on that section's own tables: filtered parameter dropdowns, parameter configuration overrides, nested submission criteria, form sections, and everything in §5.2's side effects.
5. ~~**Time series and media reference property types.**~~ — decision 0009 made, and both built as far as this platform can honour them. Media needed no type at all, only a renderer (§147). Time series is the type, the `object_type_series` mapping and the points read (§148); what is left is the *chart* that draws them, which belongs with §4.1's standard Object View rather than with the storage.
6. ~~**Configured Object Views**, reusing the Workshop runtime~~ — **done (`STATUS.md` §144)**. A pointer at a published module plus the one variable that receives the object; the panel form factor is stored and separately addressable, and waits for something to embed it in.
7. ~~**Struct property type**~~, ~~then the Ontology Manager editor~~, ~~then **Workshop struct variables**~~. — **done: the type (§245), the editor (§246) and the variable with p.143's extract (§247)**. Struck in thirds rather than whole, because §216's rule is that a line goes stale in the commit that finishes part of it. The variable and the transform were one item rather than two, and p.155 is why: "widgets and variable transformation operations cannot use structs as a whole, so individual struct fields must be extracted for use" makes the variable useless without the transform. **What is left of structs is not on this line**: struct *arrays* are `workshop.md` §1.3's loop row, and p.59's main fields and reducers are on §1.1's own row — both are about arrays of structs, which is a property type this platform does not have. **Reading the pages first changed the shape of this line** — it reads as one item and it is at least three, which is the direction every build-order estimate in this repo has been wrong in (§232, §235).
8. ~~**Ontology Manager search**~~ — **done (`STATUS.md` §146)**, and this line went unstruck for eighty-odd units while saying so in its own first clause (§236's audit). Across six kinds: object types, properties, link types, action types, shared properties (§167) and groups (§172). **Filtering by group is done** (§172, p.262), and **so are p.29's other two** — visibility and development status, §250. The line used to name only one of those two, which is what re-reading the page it cites turned up: `ontology-manager` p.29 lists three filters, not two. The **indexing-issue column** is the third and is still open — it wants indexing state the sync path does not record, so there is nothing to filter on rather than nothing built.
9. ~~Shared properties~~, ~~value types~~, ~~**interfaces**~~, ~~derived properties~~ — **all four are built and ◑** (interfaces in §251, which is the "explicit decision to design it from the fragments and mark what was guessed" that §1.2's row had been asking for since §168 — the guesses are named on that row — with the Ontology Manager surface in §253, which also closed a gap §251 left: p.256's status gate applied to every other ontology resource and not to that one). Previously: **three of the four are built and ◑** (`STATUS.md` §161–§163 derived, §164/§167 shared, §168 value types); each is ◑ for reasons its own row in §1.2 names. Interfaces were the one that had not started, and §1.2 still marks them `[?]`: the gap is a missing chapter rather than a scheduling decision, so what §251 built is the model the fragments settle, with everything inferred listed on the row.
10. ~~Side effects — notifications, then webhooks.~~ — **done, both halves.** *Notifications* (`STATUS.md` §257–§258): the sixth `action_rule_kind` and its delivery, then its editor and the recipient picker. *Webhooks* (§259–§260, decision 0012): the resource on a REST source, then the action rule in `action-types` p.106's two modes — writeback before the write and able to refuse it, side effect after and unable to. Load-bearing twice as this line said it would be, and one implementation served both: `data-connection.md` item 2 is struck by the same two units. **Struck in halves as it went**, per §216 — a line goes stale in the commit that finishes part of it, and this one still claimed webhooks were outstanding after §260 had merged them, which is the drift §216 exists to catch. What each feature does *not* do is on its own row in §5.2 rather than here.
11. ~~**The object type listing is unbounded, and that is ours rather than a parity gap.**~~ — **done (`STATUS.md` §256)**. The endpoint is a page of 50 with a total, a search and an `ids` filter; the seven single-select pickers became one searchable `TypePicker`; the Ontology Manager's table gained a search and paging; and the two per-row lookups under it (`groups_by_type`, `implementations_by_type`) are narrowed to the page's ids, because one query for the workspace under a bounded listing is the same defect one query down. Measured on the 791-type development workspace: 253.8ms/379.5KB unbounded against 17.4ms/23.9KB for a page. The original text follows, because the argument in it is why this was a unit rather than a `LIMIT`.
    `ontology.list_types` has no `LIMIT`, so the Ontology Manager's table fetches and renders
    every object type in the workspace — and so does every type *picker* built on the same
    endpoint (eight call sites). Found in §209, where a dev workspace holding ~1,400 types made
    the listing take seven seconds to open a dialog and turned a browser test red. p.262 says
    only that the table "supports displaying and filtering by group", so there is no page to
    copy a page size from; the shape is ours to choose. **The pickers are what make it a unit
    rather than a one-line `LIMIT`**: a dropdown that silently truncates is worse than a slow
    one, so bounding the endpoint means making them searchable at the same time. Until then the
    listing is O(the ontology), which is survivable for a real one and was not for a dev
    workspace full of fixtures.

**This list was audited in §216, and three of its eleven entries were wrong.** Items 2, 4 and 9
each described work that had since been done — item 4 for sixteen units, while a recommendation
about what to build next was made *from* it. The failure is not carelessness: a build order records
what was true when it was written, and nothing in the ordinary flow of work ever re-reads it.
Finishing an item means opening a pull request, not editing this list, so the list drifts in
exactly one direction — it always claims *more* is left than there is.

Two things follow, and they are cheap. **Finishing a unit means striking its line here**, in the
same commit; and **before recommending anything from this list, open the thing it cites** — a
decision record's own Status line, or the section's table. Both of §216's worst cases would have
been caught by reading one sentence: decisions 0007 and 0008 both begin "decided and **built**".

`workshop.md`'s build order was audited in the same pass and had two wrong entries. The other
three — `code-repositories.md`, `datasets-lineage.md`, `data-connection.md` — were checked and are
accurate, which is what you would expect: **no unit has been built against them**, so nothing has
had the chance to drift. That is the shape of this failure. A build order is only wrong where work
has been done, so the lists that look most trustworthy are the ones nobody has touched.


---

## 8. Acceptance tests

- **Property visibility** — a hidden property is absent from the standard Object View and from the Object Explorer's columns. Mutation: mark it normal, and both change. **Note on the original wording**: this said "the Object Table column picker", and there is no picker — the Workshop Object Table takes a typed `columns` string. The Explorer's derived columns are the equivalent surface and are what the test drives.
- **Standard Object Views** — an object type with a prominent time series property renders a chart, not a table cell; with a prominent geopoint, a map. Remove the prominent flag and it falls back to the table.
- **Linked objects** — an instance with two link types shows two groups; expanding one previews properties without navigation. **Met (§145)**, in `e2e/test_linked_object_preview.py`: the trail is the evidence nothing navigated, and the mutation that draws every declared property regardless of visibility is red.
- **Configured views** — creating one makes it the default; the standard view is still reachable. **Met (§144)**, in `e2e/test_configured_object_view.py`: the module renders in place of the generated view, the object reaches it through `object_property` on the subject variable (so a module that renders without the object goes red), and the switch works both ways.
- **Link sides** — a self-link between Employee and Employee renders both directions with distinct names.
- **Action parameters** — an action with a parameter whose submission criteria fail is **refused**, and the refusal names the criterion. Mutation: remove the criterion check, and the test goes red.
- **Action transactions** — an action editing two objects where the second edit fails leaves **neither** applied.
- **Undo** — reverting an action restores prior property values and re-creates deleted links. **Half met (§319)**, and the half named rather than blurred: prior property values are restored, in `apps/api/tests/test_action_undo.py` — through the dataset as well as the index, with a re-sync afterwards, because restoring only the projection would look perfect and come back at the next sync. Deletions are **not** re-created: a run that deleted or created objects records why it can never be undone and the button is absent with that sentence in its place, which is §214's argument that an undo half-working is worse than one that says it cannot. p.156 treats reverting a delete as supported, so this line stays as a target.
- **Usage metrics** — a bulk load of 500 objects records **one** read, not 500. **Met (§320)**, in `apps/api/tests/test_object_type_usage.py`: a page of objects counts as one read, and the mutation that counts per row rather than per request goes red. The page size in the test is two rather than five hundred — the rule is what is under test, and a fixture of five hundred rows would be slower without being a stronger claim. p.32's other sentence, that the Ontology Manager's own usage is not counted, is in `e2e/test_usage_metrics.py`, because it is a claim about which screen you are on and both screens use the same route.
